---
wave: 1
depends_on: []
files_modified:
  - src/components/downloader/DownloaderPanel.tsx
  - src/components/downloader/DouyinAuthWidget.tsx
  - src/components/downloader/URLInput.tsx
  - src/components/downloader/DownloadOptions.tsx
  - src/styles/downloader.css
autonomous: false
---

# Plan 07.1: Stitch UI & WebView Auth

## Goal
Implement a premium asymmetrical design for the Downloader and add a WebView-based Douyin Login feature.

## Tasks

### [Task 07.1-01] Refactor `downloader.css` to Stitch Standards
<read_first>
- src/styles/downloader.css
- .agent/skills/taste-skill/skills/stitch-skill/DESIGN.md
</read_first>
<action>
Modify `src/styles/downloader.css`:
- Update `--dl-bg` to `#F9FAFB`, `--dl-text` to `#18181B`, `--dl-border` to `rgba(226,232,240,0.5)`.
- Use font `Outfit` (or `Geist`) and apply `letter-spacing: -0.025em` for headings.
- Add CSS variables for spring animation: `--spring-smooth: cubic-bezier(0.175, 0.885, 0.32, 1.275)`.
- Redefine layout grid to support asymmetric split-screen (e.g., `grid-template-columns: 1fr 1fr` or similar uneven fractions).
- Ensure cards have `border-radius: 2.5rem`.
</action>
<acceptance_criteria>
- `downloader.css` contains `#18181B`, `cubic-bezier(0.175, 0.885, 0.32, 1.275)` and grid layout declarations.
</acceptance_criteria>

### [Task 07.1-02] Implement `DouyinAuthWidget.tsx` and Tauri WebView
<read_first>
- src/components/downloader/CookieManager.tsx
</read_first>
<action>
Create `src/components/downloader/DouyinAuthWidget.tsx`:
- This component replaces `CookieManager.tsx`.
- Include a button labeled "Login Douyin" that triggers `WebviewWindow` from `@tauri-apps/api/window` pointing to `https://www.douyin.com/` (or via Tauri commands).
- Render a premium "Session Connected" status if logged in.
- Hook up the sidecar endpoints to read/write the cookie fetched from Tauri.
</action>
<acceptance_criteria>
- `DouyinAuthWidget.tsx` contains `DouyinAuthWidget` export and Tauri API usage.
</acceptance_criteria>

### [Task 07.1-03] Refactor Downloader React Components to Asymmetric Layout
<read_first>
- src/components/downloader/DownloaderPanel.tsx
- src/components/downloader/URLInput.tsx
- src/components/downloader/DownloadOptions.tsx
</read_first>
<action>
- Modify `DownloaderPanel.tsx`: Change layout from centered flex block to a split `div` containing `URLInput` on one side and `DownloadHistory` on the other, placing `DouyinAuthWidget` prominently.
- Modify `URLInput.tsx`: Make button tactile (active state: `-1px translateY`) and remove excessive shadows. Use deep rose `#E11D48` for error states.
- Modify `DownloadOptions.tsx`: Adopt `2.5rem` border radius for thumbnails and utilize cascading delay animations.
</action>
<acceptance_criteria>
- `DownloaderPanel.tsx` no longer uses `.dl-content-centered`.
</acceptance_criteria>

## Verification
- Run `npm run tauri dev`.
- Verify the WebView auth popup opens correctly.
- Verify the layout is asymmetric and typography matches Stitch guidelines.

## Must Haves
- WebView Login functional
- Stitch UI variables applied
