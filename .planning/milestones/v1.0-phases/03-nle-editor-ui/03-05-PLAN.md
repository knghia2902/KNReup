# Plan 03-05: Gap Closures & Fixes

## 1. Pipeline Completion (Gap 1)
- Trigger completion state correctly in `App.tsx` and stop loader.
- Add Reset mechanism.

## 2. Multi-File Media Bin (Gap 3)
- `App.tsx`: `const [filePaths, setFilePaths] = useState<string[]>([])`.
- `UploadPanel.tsx`: Map array items.

## 3. Video Controls (Gap 4)
- `VideoControls.tsx`: Implement skip forward/backwards buttons or trim unused buttons to match intended MVP state.

## 4. Properties Reactivity (Gap 2 & 5)
- `VideoPreview.tsx`: Add `useEffect` on `subtitleConfig` to invoke `renderSubtitles()` while the video is paused. Connect `video_ratio` slider to update `.vframe` container width/height max sizes using React styles overriding the static CSS queries.

## 5. Timeline Dynamic Data (Gap 6)
- `TimelinePlaceholder.tsx`: Compute actual max duration block width and map segments to track elements correctly.
