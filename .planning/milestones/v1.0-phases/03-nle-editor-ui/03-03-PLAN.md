---
description: "Wave 3: Video Preview & Subtitle Canvas Overlay"
wave: 3
depends_on: ["03-01-PLAN.md"]
files_modified: 
  - "src/components/editor/VideoPreview.tsx"
  - "src/components/editor/VideoPreview.css"
  - "src/components/editor/VideoControls.tsx"
autonomous: true
---

# Phase 3, Plan 3: Video Preview & Subtitle Canvas Overlay

<objective>
Tái cấu trúc cụm Viewport Video bằng mẫu kiến trúc từ HTML tĩnh. Đảm bảo `<video>` và `<canvas>` xếp đè lên nhau chuẩn xác bằng CSS Grid `1 / 1`. Render thanh Playback Controls custom.
</objective>

<requirements>
- Video Preview panel: playback controls, WYSIWYG subtitle overlay (Canvas API)
- Canvas font rendering tiếng Việt: FontFaceObserver, kiểm tra dấu render đúng
</requirements>

<tasks>
  <task>
    <read_first>
      - d:\Tools\KNReup\knreup-complete.html
      - src/components/editor/VideoPreview.tsx
      - src/components/editor/VideoPreview.css
    </read_first>
    <action>
      1. Sửa style của `VideoPreview.css` (bám sát DOM class `.pvp`, `.pvbody`, `.vframe`, `.vinner`, `.vnolbl`). Chèn logic CSS Grid Layering cho video và canvas (grid-area: 1 / 1; min-height/width 0) để chống bung khung (Bug Webview2 absolute).
      2. Trong `VideoPreview.tsx`, nếu chưa có Video thì render DOM Empty State: `<div className="vnolbl">Drop a video here...</div>` (kèm scanlines effect).
      3. Nếu có video, render DOM giống HTML:
         ```tsx
         <div className="vframe">
           <div className="vinner">
             <video ... />
             <canvas ... />
           </div>
         </div>
         ```
      4. Sử dụng class `.vtc` cho timecode góc trên trái màn hình `HH:MM:SS:FF`.
    </action>
    <acceptance_criteria>
      - `VideoPreview.css` contains variables/classes like `.vframe`, `.vinner`
      - `VideoPreview.tsx` renders `<video>` and `<canvas>` in a Grid layout to prevent Webview2 bugs
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - d:\Tools\KNReup\knreup-complete.html
      - src/components/editor/VideoControls.tsx
    </read_first>
    <action>
      1. Lấy toàn bộ giao diện thanh dưới Preview `.pvctrl` từ HTML mẫu.
      2. Xóa các file cũ và tạo `VideoControls.tsx` tuân thủ DOM:
         - Nút Play/Pause `.cb.play`
         - Các nút tua/volume/settings `.cb`
         - Thanh scrubber `.scrub > .scrubf` (đại diện Timeline mini của player).
         - Hiển thị Text Timecode hiện tại `.tcd`.
      3. Đấu nối các sự kiện `onClick` vào `videoRef` để Play/Pause và Sync thanh Scrubber `%` hiện tại với `video.currentTime`.
    </action>
    <acceptance_criteria>
      - `VideoControls` exports successfully
      - Play/Pause buttons correctly call `videoRef.current.play()` / `pause()`
    </acceptance_criteria>
  </task>
</tasks>

<verification>
### Manual UI Check
1. Chạy App. Load 1 video sample.
2. Kiểm tra thanh Video Controls (giao diện mới màu Dark/Beige) có hoạt động Play/Pause chuẩn xác không.
3. Box Video không bị phình to (nhờ giới hạn min-height 0 của Grid item).
</verification>

<must_haves>
- Custom Playback controls phải đẹp 100% như Design HTML.
- Video + Canvas không bao giờ làm tràn (overflow) layout NLELayout.
</must_haves>
