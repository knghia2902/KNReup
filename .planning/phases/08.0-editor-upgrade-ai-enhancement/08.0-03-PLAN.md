---
phase: 08.0-editor-upgrade-ai-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/components/editor/Timeline.tsx",
  "src/stores/useProjectStore.ts"
]
autonomous: true
requirements: [M3-ED-01]
---

<objective>
Implement Smart Snapping (Magnetic Timeline), Multi-select, and Selection Sync between Timeline and Properties.

Purpose: Professionalize the editor interaction to match industry standards like CapCut.
Output: Enhanced Timeline component with snapping and advanced selection logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/editor/Timeline.tsx
@src/stores/useProjectStore.ts
@.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-CONTEXT.md
@.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Smart Snapping logic</name>
  <files>src/components/editor/Timeline.tsx</files>
  <action>
    - Implement a proximity check in the dragging logic.
    - Snap targets: Playhead position, Start/End of all existing clips on all tracks.
    - Threshold: 12px (D-03).
    - Draw a vertical "Alignment Guide" line (lime/green color) when a snap occurs.
  </action>
  <verify>
    <automated>Check for snap-related logic and guide line in Timeline.tsx</automated>
  </verify>
  <done>Smart Snapping implemented with visual feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-select and Selection Sync</name>
  <files>src/components/editor/Timeline.tsx, src/stores/useProjectStore.ts</files>
  <action>
    - Update `useProjectStore` to support `selectedClipIds: string[]` instead of single `selectedClipId`.
    - Implement `Ctrl + Click` / `Shift + Click` for multi-selection on the timeline.
    - Selection Sync (D-01): When a clip is clicked, determine its track type (VID/TTS/SUB/AUDIO) and automatically switch the Properties Panel tab (STYLE/TTS/SUB/OUT) accordingly.
  </action>
  <verify>
    <automated>Verify selectedClipIds array and tab switching logic.</automated>
  </verify>
  <done>Multi-select and Selection Sync implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Trim/Offset interaction for all tracks</name>
  <files>src/components/editor/Timeline.tsx</files>
  <action>
    - Ensure all 4 tracks (VID, TTS, SUB, AUDIO) support basic drag-to-move and handle-resize-to-trim interactions.
    - For TTS/AUDIO, updating the timeline start/end should update the store's offset and duration values.
  </action>
  <verify>
    <automated>Check for event handlers on all track types.</automated>
  </verify>
  <done>Full track interaction enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | Interaction events (drag, click) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.0-04 | Denial of Service | Rapid Snapping Calculations | mitigate | Debounce or throttle snap calculations during high-frequency drag events if performance drops. |
</threat_model>

<verification>
- Drag a clip and confirm it "hits" the playhead or another clip's edge with a green line.
- Select multiple clips and confirm they are highlighted.
- Click a TTS clip and confirm the Properties Panel switches to the TTS tab.
</verification>

<success_criteria>
- Timeline has "Magnetic" feel.
- Multi-selection works.
- Selection syncs with Properties Panel.
</success_criteria>

<output>
After completion, create `.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-03-SUMMARY.md`
</output>
