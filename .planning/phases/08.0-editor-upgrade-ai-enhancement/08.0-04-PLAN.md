---
phase: 08.0-editor-upgrade-ai-enhancement
plan: 04
type: execute
wave: 2
depends_on: ["08.0-01"]
files_modified: [
  "python-sidecar/requirements.txt",
  "python-sidecar/app/routes/pipeline.py",
  "python-sidecar/app/engines/ai/refiner.py",
  "python-sidecar/app/engines/ai/masking.py",
  "python-sidecar/app/engines/ai/diarization.py"
]
autonomous: true
requirements: [M3-ED-02, M3-ED-03, M3-ED-04]
---

<objective>
Implement AI Subtitle Refiner (LLM-based), AI Subtitle Masking (EasyOCR-based), and Speaker Diarization endpoints in the sidecar.

Purpose: Automate tedious editing tasks using AI.
Output: New sidecar endpoints for subtitle optimization, hardsub detection, and speaker identification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@python-sidecar/app/routes/pipeline.py
@.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-CONTEXT.md
@.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI dependencies</name>
  <files>python-sidecar/requirements.txt</files>
  <action>
    - Add `easyocr` and `pyannote.audio` (for speaker diarization) to `requirements.txt`.
    - Note: These models are large and will download on first run.
  </action>
  <verify>
    <automated>pip install easyocr pyannote.audio</automated>
  </verify>
  <done>AI dependencies added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI Refiner engine</name>
  <files>python-sidecar/app/engines/ai/refiner.py</files>
  <action>
    - Create `refiner.py`.
    - Implement a `SubtitleRefiner` class that uses an LLM (Gemini or OpenAI via existing engines) to proofread and optimize subtitle segments.
    - Context-aware prompt: "Refine these subtitles for a [style] video, fix typos and improve flow."
  </action>
  <verify>
    <automated>Check for SubtitleRefiner class logic.</automated>
  </verify>
  <done>AI Refiner engine implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AI Masking engine</name>
  <files>python-sidecar/app/engines/ai/masking.py</files>
  <action>
    - Create `masking.py`.
    - Implement an `AutoMasker` class using `EasyOCR`.
    - Logic: Scan frames at regular intervals (e.g., every 1s), detect text boxes in the lower 30% of the screen, and group them into temporal regions (D-08).
    - Return a list of `blur_regions` (x, y, w, h, start, end).
  </action>
  <verify>
    <automated>Check for EasyOCR integration in masking.py</automated>
  </verify>
  <done>AI Masking engine implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Speaker Diarization engine</name>
  <files>python-sidecar/app/engines/ai/diarization.py</files>
  <action>
    - Create `diarization.py`.
    - Implement a `SpeakerDiarizer` class using `pyannote.audio`.
    - Logic: Identify unique speakers and their timestamps in the audio track.
    - Output: Mapping of segment IDs to speaker labels (Speaker 01, Speaker 02...).
  </action>
  <verify>
    <automated>Check for pyannote integration in diarization.py</automated>
  </verify>
  <done>Speaker Diarization engine implemented.</done>
</task>

<task type="auto">
  <name>Task 5: Register AI endpoints</name>
  <files>python-sidecar/app/routes/pipeline.py</files>
  <action>
    - Add `/pipeline/ai-refine` (POST) endpoint.
    - Add `/pipeline/ai-mask` (POST) endpoint.
    - Add `/pipeline/ai-diarize` (POST) endpoint.
  </action>
  <verify>
    <automated>Check for new endpoints in pipeline.py</automated>
  </verify>
  <done>AI endpoints registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Sidecar -> LLM API | Subtitle content sent to external AI |
| Sidecar -> Local AI | Video/Audio processed locally for OCR and Diarization |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.0-05 | Privacy | Subtitle Content in LLM | accept | User initiates the action; standard LLM API privacy terms apply. |
| T-08.0-06 | Denial of Service | OCR/Diarization Load | mitigate | Process AI tasks in the background to avoid blocking the sidecar. |
</threat_model>

<verification>
- Call `/api/pipeline/ai-refine` with a sample segment and confirm text is improved.
- Call `/api/pipeline/ai-mask` with a short video and confirm it returns bounding boxes.
- Call `/api/pipeline/ai-diarize` and confirm it identifies speakers.
</verification>

<success_criteria>
- AI Refiner successfully optimizes text using LLM.
- AI Masking detects hardsubs and returns valid coordinates.
- Speaker Diarization identifies unique speakers.
</success_criteria>

<output>
After completion, create `.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-04-SUMMARY.md`
</output>
