---
phase: 08.0-editor-upgrade-ai-enhancement
plan: 01
subsystem: sidecar
tags: [tts, omnivoice, elevenlabs, python]
requires: []
provides: [omnivoice-engine, elevenlabs-engine, tts-profiles]
affects: [app/routes/pipeline.py, app/main.py, app/engines/tts/piper_engine.py]
tech-stack:
  added: [omnivoice, elevenlabs, librosa, pydub]
  patterns: [adapter, factory]
key-files:
  created:
    - python-sidecar/app/engines/tts/omnivoice_engine.py
    - python-sidecar/app/engines/tts/elevenlabs_engine.py
    - python-sidecar/app/routes/tts_profiles.py
  modified:
    - python-sidecar/requirements.txt
    - python-sidecar/app/routes/pipeline.py
    - python-sidecar/app/main.py
  deleted:
    - python-sidecar/app/engines/tts/piper_engine.py
decisions:
  - "Use ElevenLabs SDK to handle cloud synthesis and mapping."
  - "Load OmniVoice model lazily to prevent high memory usage when not in use."
metrics:
  duration: "5m"
  tasks_completed: 3
  files_changed: 6
---

# Phase 08.0 Plan 01: Sidecar TTS Integration Summary

Integrated OmniVoice and ElevenLabs TTS engines into the sidecar, removed Piper, and implemented voice profile endpoints.

## Implementation Details

1. **Updated Dependencies:** Replaced `piper-tts` with `omnivoice`, `elevenlabs`, `pydub`, and `librosa` in `requirements.txt`.
2. **Engines Implemented:**
   - Created `OmniVoiceTTSEngine` with lazy model loading and `speed` mapping. Added voice cloning methods (`preprocess_reference_audio` and `create_voice_profile`).
   - Created `ElevenLabsTTSEngine` integrated with the official `elevenlabs` client, passing `api_key` appropriately.
3. **API Enhancements:**
   - Created `/api/tts/profiles` router with endpoints for reference upload and profile creation.
   - Updated `pipeline.py` TTS synthesis endpoints to accept `speed`, `profile_name`, and `api_key`, routing dynamically to the new engines.
4. **Clean-up:** Removed `piper_engine.py`.

## Deviations from Plan
- None - plan executed exactly as written.
- Added strict file extension validation in `/upload-reference` and regex sanitization in `/save` to mitigate T-08.0-03 (Spoofing/Tampering).

## Threat Flags
*(None - security mitigations successfully implemented)*

## Self-Check: PASSED
- `python-sidecar/app/engines/tts/omnivoice_engine.py` exists
- `python-sidecar/app/engines/tts/elevenlabs_engine.py` exists
- `python-sidecar/app/routes/tts_profiles.py` exists
- `python-sidecar/app/engines/tts/piper_engine.py` deleted
- Commits recorded properly
