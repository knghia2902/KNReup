---
phase: 08.0-editor-upgrade-ai-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["08.0-01"]
files_modified: [
  "src/stores/useProjectStore.ts",
  "src/components/properties/AudioTab.tsx",
  "src/components/properties/SubtitleTab.tsx",
  "src/lib/sidecar.ts"
]
autonomous: true
requirements: [M3-ED-02]
must_haves:
  truths:
    - "UI allows selection of OmniVoice and ElevenLabs engines, and Piper is removed."
    - "UI provides a voice cloning interface for OmniVoice to upload audio, preprocess, and save profiles."
    - "Users can adjust global and per-segment TTS speed."
  artifacts:
    - path: "src/stores/useProjectStore.ts"
      provides: "State for ElevenLabs API key, voice mappings, and TTS speed"
    - path: "src/components/properties/AudioTab.tsx"
      provides: "TTS engine selection, API key input, and voice cloning UI"
  key_links:
    - from: "src/components/properties/AudioTab.tsx"
      to: "src/lib/sidecar.ts"
      via: "voice cloning API calls"
---

<objective>
Update the frontend to support OmniVoice and ElevenLabs TTS engines, remove Piper TTS from the UI, and implement advanced voice cloning and parameter adjustment UI.

Purpose: Provide a modern UI for the new TTS engines, enable advanced per-segment dubbing, and allow users to create and manage custom cloned voice profiles with OmniVoice.
Output: Updated store, sidecar API bindings, and UI components in the Properties panel.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/stores/useProjectStore.ts
@src/components/properties/AudioTab.tsx
@src/components/properties/SubtitleTab.tsx
@src/lib/sidecar.ts
@.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update store and API bindings</name>
  <files>src/stores/useProjectStore.ts, src/lib/sidecar.ts</files>
  <action>
    - In `useProjectStore.ts`:
      - Add `elevenlabs_api_key` to `ProjectConfig` and `DEFAULT_CONFIG`. Exclude it from file-specific overrides.
      - Add `tts_speed` (default 1.0) to `ProjectConfig`.
      - Add `voice_mapping` record to `ProjectConfig` to store per-segment overrides: `Record<string, { engine?: string, voice?: string, speed?: number }>`.
      - Add `custom_voice_profiles` array to state to cache loaded OmniVoice profiles.
    - In `sidecar.ts`:
      - Add methods for `/api/tts/profiles/upload-reference`, `/api/tts/profiles/save`, and `/api/tts/profiles` (GET).
      - Ensure the `synthesize` API call includes `speed` and `profile_name` parameters.
  </action>
  <verify>
    <automated>npm run tsc -- --noEmit</automated>
  </verify>
  <done>Store updated with new TTS fields and API bindings implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update AudioTab UI for TTS Engines & Voice Cloning</name>
  <files>src/components/properties/AudioTab.tsx</files>
  <action>
    - Modify the TTS engine grid: Add `OmniVoice` and `ElevenLabs`, remove `Piper`.
    - Add an input field for `ElevenLabs API Key` (password type) when ElevenLabs is selected.
    - Add a `TTS Speed` slider (range 0.5 to 2.0).
    - When `OmniVoice` is selected, add a "Voice Cloning" section:
      - File input to upload a reference audio file.
      - Button to "Preprocess Audio" (calls sidecar API).
      - Text input for "Profile Name" and a "Save Profile" button.
      - Dropdown to select from existing custom profiles or standard voices.
  </action>
  <verify>
    <automated>npm run tsc -- --noEmit</automated>
  </verify>
  <done>AudioTab UI updated with engine selection, speed control, and voice cloning interface.</done>
</task>

<task type="auto">
  <name>Task 3: Implement per-segment voice selection</name>
  <files>src/components/properties/SubtitleTab.tsx</files>
  <action>
    - Add a "Voice" selector and "Speed" input to each subtitle segment in the list.
    - This allows overriding the global engine/voice/speed for that specific segment.
    - Store the override in `voice_mapping` in `useProjectStore`.
    - Add an "Apply to All" button to sync a segment's voice/speed to global settings.
  </action>
  <verify>
    <automated>npm run tsc -- --noEmit</automated>
  </verify>
  <done>Per-segment voice and speed selection implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | User enters API key and uploads audio |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.0-03 | Information Disclosure | ElevenLabs API Key in UI | mitigate | Use password-type input and do not display key in plain text. |
| T-08.0-04 | Spoofing | File Upload | mitigate | Restrict file input `accept` attribute to audio formats (e.g., `audio/*`). |
</threat_model>

<verification>
- Select OmniVoice: confirm cloning UI appears.
- Test uploading an audio file and saving a profile name.
- Select ElevenLabs: enter key and confirm UI updates.
- Adjust global speed slider and confirm state updates.
- Change voice and speed for a specific subtitle segment and verify it persists.
</verification>

<success_criteria>
- Frontend supports OmniVoice with cloning UI and ElevenLabs.
- Piper is completely removed from UI.
- Users can create, save, and select custom voice profiles.
- Per-segment voices and speeds can be configured.
</success_criteria>

<output>
After completion, create `.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-02-SUMMARY.md`
</output>