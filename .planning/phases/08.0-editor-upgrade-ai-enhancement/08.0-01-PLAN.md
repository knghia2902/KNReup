---
phase: 08.0-editor-upgrade-ai-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "python-sidecar/requirements.txt",
  "python-sidecar/app/engines/tts/omnivoice_engine.py",
  "python-sidecar/app/engines/tts/elevenlabs_engine.py",
  "python-sidecar/app/routes/pipeline.py",
  "python-sidecar/app/routes/tts_profiles.py"
]
autonomous: true
requirements: [M3-ED-02]
must_haves:
  truths:
    - "OmniVoice engine is available and Piper is removed."
    - "ElevenLabs engine is available."
    - "User can clone a voice from a reference audio file and save it as a profile."
    - "User can adjust TTS speed parameter."
  artifacts:
    - path: "python-sidecar/app/engines/tts/omnivoice_engine.py"
      provides: "OmniVoice TTS and voice cloning integration"
    - path: "python-sidecar/app/routes/tts_profiles.py"
      provides: "Endpoints for managing custom voice profiles"
  key_links:
    - from: "python-sidecar/app/routes/tts_profiles.py"
      to: "python-sidecar/app/engines/tts/omnivoice_engine.py"
      via: "voice cloning methods"
---

<objective>
Integrate OmniVoice (Offline) and ElevenLabs (Cloud) TTS engines into the sidecar, remove the Piper TTS engine, and implement deep customization for OmniVoice.

Purpose: Provide higher quality local TTS with OmniVoice and professional cloud TTS with ElevenLabs. Implement advanced OmniVoice features like voice cloning, profile saving, reference audio preprocessing, and parameter adjustments (e.g., speed).
Output: Working OmniVoice and ElevenLabs integrations in the Python sidecar, including voice profile management endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@python-sidecar/requirements.txt
@python-sidecar/app/engines/base.py
@python-sidecar/app/routes/pipeline.py
@python-sidecar/app/engines/tts/piper_engine.py

<interfaces>
From python-sidecar/app/engines/base.py:
```python
class TTSEngine(ABC):
    engine_name: str = "unknown"
    is_online: bool = True
    @abstractmethod
    async def synthesize(self, text, voice, output_path, rate=1.0, volume=1.0, pitch=0.5) -> str: ...
    @abstractmethod
    async def list_voices(self) -> list[dict]: ...
    @abstractmethod
    async def health_check(self) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidecar dependencies</name>
  <files>python-sidecar/requirements.txt</files>
  <action>
    - Add `omnivoice` and `elevenlabs` to `requirements.txt`.
    - Remove `piper-tts`.
    - Add any necessary dependencies for audio preprocessing (e.g., `pydub`, `librosa`) if OmniVoice doesn't handle it natively.
    - Note: OmniVoice requires PyTorch. Ensure it matches the project's CUDA environment if applicable.
  </action>
  <verify>
    <automated>cd python-sidecar && pip install -r requirements.txt</automated>
  </verify>
  <done>Dependencies updated and installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OmniVoice and ElevenLabs engines</name>
  <files>python-sidecar/app/engines/tts/omnivoice_engine.py, python-sidecar/app/engines/tts/elevenlabs_engine.py</files>
  <action>
    - Create `omnivoice_engine.py`:
      - Implement `OmniVoiceTTSEngine` inheriting from `TTSEngine`.
      - Use `omnivoice` library for synthesis. Implement support for the `speed` parameter (mapping it to the engine's `rate` or similar argument).
      - Implement methods for voice cloning: `preprocess_reference_audio(input_path, output_path)` and `create_voice_profile(audio_path, profile_name)`.
      - Manage loading and listing custom voice profiles from a local directory (e.g., `data/tts_profiles`).
      - Use `app.utils.gpu_detect` for device selection (force CUDA if available).
    - Create `elevenlabs_engine.py`:
      - Implement `ElevenLabsTTSEngine` inheriting from `TTSEngine`.
      - Require an `api_key` in constructor.
      - Implement `synthesize` using the `text_to_speech` method and saving to file.
      - Implement `list_voices` using the ElevenLabs API.
  </action>
  <verify>
    <automated>pytest python-sidecar/tests/test_engines.py (if exists, or confirm syntactically valid via python -m py_compile)</automated>
  </verify>
  <done>Engine classes implemented with voice cloning and parameter support.</done>
</task>

<task type="auto">
  <name>Task 3: Register engines and add profile endpoints</name>
  <files>python-sidecar/app/routes/pipeline.py, python-sidecar/app/routes/tts_profiles.py</files>
  <action>
    - In `pipeline.py`:
      - Update `get_tts_engine` factory to include `omnivoice` and `elevenlabs`, and remove `piper`.
      - Update `TTSRequest` model to include `speed` (float, default 1.0) and `profile_name` (optional string).
      - Ensure the `synthesize` endpoint passes these parameters to the engine.
    - Create `tts_profiles.py`:
      - Implement new API routes under `/api/tts/profiles`.
      - `POST /upload-reference`: Accepts an audio file, runs `preprocess_reference_audio`.
      - `POST /save`: Accepts the preprocessed audio path and a profile name, calls `create_voice_profile` in OmniVoice engine.
      - `GET /`: Lists all saved voice profiles.
    - Register the new router in the main app (assuming `main.py` dynamic inclusion or update it if needed).
  </action>
  <verify>
    <automated>python -c "import python_sidecar.app.routes.tts_profiles"</automated>
  </verify>
  <done>Engines registered, Piper removed, and profile management endpoints created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend -> Sidecar | Audio files and parameters sent for cloning |
| Sidecar -> ElevenLabs | API key and text sent to external cloud service |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.0-01 | Information Disclosure | ElevenLabs API Key | mitigate | Store API key in memory/Zustand, do not log it in sidecar. |
| T-08.0-02 | Denial of Service | OmniVoice Inference | mitigate | Use a semaphore or queue to prevent concurrent heavy local TTS inference. |
| T-08.0-03 | Spoofing/Tampering | File Upload for Cloning | mitigate | Validate uploaded file types (only allow audio formats) and sanitize profile names to prevent directory traversal. |
</threat_model>

<verification>
- Confirm `omnivoice` and `elevenlabs` can synthesize audio via sidecar endpoints.
- Confirm `piper` is no longer available in the sidecar registry.
- Test voice cloning flow: upload reference audio -> preprocess -> save profile.
</verification>

<success_criteria>
- Sidecar responds to `/api/pipeline/voices?engine=omnivoice`.
- Sidecar responds to `/api/pipeline/voices?engine=elevenlabs` (with valid key).
- Custom voice profiles can be created and listed.
- Synthesis respects the `speed` parameter and custom profiles.
</success_criteria>

<output>
After completion, create `.planning/phases/08.0-editor-upgrade-ai-enhancement/08.0-01-SUMMARY.md`
</output>