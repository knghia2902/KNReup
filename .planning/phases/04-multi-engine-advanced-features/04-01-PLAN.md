---
phase: 4
plan_id: 01
wave: 1
depends_on: []
files_modified:
  - python-sidecar/app/engines/translation/gemini.py
  - python-sidecar/app/engines/translation/openai_engine.py
  - python-sidecar/app/engines/tts/gtts_engine.py
  - python-sidecar/app/engines/tts/smartvoice.py
  - python-sidecar/app/engines/base.py
  - python-sidecar/app/routes/pipeline.py
autonomous: false
requirements: [M2-01, M2-02, M2-03, M2-04]
---

# Phase 4 - Plan 01: Multi-Engine & Circuit Breaker

<objective>
Tích hợp các engine dịch thuật và TTS mới (OpenAI API, gTTS, SmartVoice). Xây dựng cơ chế Circuit Breaker (Xoay vòng API Key và Fallback tự động giữa các engine) khi hệ thống gặp lỗi Rate Limit hoặc Server Error.
</objective>

<tasks>

## Task 1: Create OpenAI Translation Engine

<action>
Create `python-sidecar/app/engines/translation/openai_engine.py` inheriting from `TranslationEngine`.
Copy `DeepSeekTranslation` implementation as a base but adapt it for `api.openai.com/v1/chat/completions`. Use model `gpt-4o-mini` by default.
Import and register it in `python-sidecar/app/routes/pipeline.py` inside `get_translation_engine`.
</action>
<read_first>
- python-sidecar/app/engines/translation/deepseek.py
- python-sidecar/app/engines/base.py
</read_first>
<acceptance_criteria>
- `python-sidecar/app/engines/translation/openai_engine.py` exists and contains `OpenAITranslation` class.
- `get_translation_engine` dynamically returns `OpenAITranslation` when engine string is `"openai"`.
</acceptance_criteria>

## Task 2: Implement Circuit Breaker and Key Rotation

<action>
Modify `get_translation_engine` in `python-sidecar/app/routes/pipeline.py` or `TranslationEngine` to support Multiple API Keys natively.
Update the function to accept a `List[str]` of api keys.
Catch `httpx.HTTPStatusError` mapping to HTTP 429. When caught, switch the `self.api_key` to the next key in the pool and retry. If all keys fail, raise an error that `pipeline_runner.py` will catch to trigger engine fallback.
</action>
<read_first>
- python-sidecar/app/engines/base.py
- python-sidecar/app/engines/translation/deepseek.py
- python-sidecar/app/routes/pipeline.py
</read_first>
<acceptance_criteria>
- Translation engines retry mechanism automatically cycles through `api_keys` list before completely failing.
- Logs correctly show "Rotating to next API key...".
</acceptance_criteria>

## Task 3: Implement Engine Fallback in Pipeline Runner

<action>
In `python-sidecar/app/pipeline_runner.py`, inside `run_analyze`, update the Stage 2 (Translate) mapping.
Wrap the `translate_segments` loop in a try-except block. If `TranslationError` is raised (meaning all keys for the primary engine failed), dynamically instantiate the `fallback_engine` (from config, e.g. "gemini" if "deepseek" fails) and continue translating from the segment that failed. Yield a `"warning"` SSE event to inform the user.
</action>
<read_first>
- python-sidecar/app/pipeline_runner.py
- d:\Tools\KNReup\.planning\phases\04-multi-engine-advanced-features\04-CONTEXT.md
</read_first>
<acceptance_criteria>
- Exception handler exists within the `translate_segments` step of `run_analyze`.
- Fallback logic checks for `config.fallback_engine` and resumes processing.
</acceptance_criteria>

</tasks>

<verification>
Create a unit test `python-sidecar/tests/test_circuit_breaker.py` that mocks `httpx.AsyncClient` to return 429 errors twice and then a 200 on the third key. Ensure the translation loop succeeds without throwing an exception.
</verification>
