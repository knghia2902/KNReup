---
phase: 11-voice-studio-full-features-text-to-speech-audio-history-ui-u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python-sidecar/app/utils/history_manager.py
  - python-sidecar/app/routes/voice_studio.py
  - python-sidecar/app/main.py
  - src/stores/useVoiceStudioStore.ts
autonomous: true
requirements:
  - M3-VS-03
  - M3-VS-04
must_haves:
  truths:
    - "Backend có thể lưu trữ và liệt kê danh sách audio history kèm metadata JSON"
    - "Frontend store có thể quản lý trạng thái đồng bộ của history từ API"
  artifacts:
    - path: "python-sidecar/app/utils/history_manager.py"
      provides: "Audio history file I/O management"
    - path: "python-sidecar/app/routes/voice_studio.py"
      provides: "API endpoints for Voice Studio history and generation"
    - path: "src/stores/useVoiceStudioStore.ts"
      provides: "Zustand store for Voice Studio state"
  key_links:
    - from: "src/stores/useVoiceStudioStore.ts"
      to: "/api/voice-studio/history"
      via: "fetch/axios"
---

<objective>
Thiết lập hạ tầng backend và frontend store để hỗ trợ việc lưu trữ và truy xuất lịch sử âm thanh (Audio History). Đây là nền tảng quan trọng để các tab chức năng có thể hiển thị danh sách các file đã tạo.

Purpose: Đảm bảo dữ liệu âm thanh được lưu trữ bền vững ở local và có thể truy cập qua API.
Output: Backend utility, API routes và Frontend store.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-CONTEXT.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-RESEARCH.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-PATTERNS.md

<interfaces>
From python-sidecar/app/routes/tts_profiles.py (Analog):
```python
@router.get("/")
async def get_profiles(): ...
```

From src/stores/useProjectStore.ts (Pattern):
```typescript
export const useProjectStore = create<ProjectState>((set, get) => ({ ... }));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Backend History Manager Utility</name>
  <files>python-sidecar/app/utils/history_manager.py</files>
  <read_first>
    - python-sidecar/app/utils/path_utils.py
    - .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-RESEARCH.md
  </read_first>
  <action>
    Tạo class `HistoryManager` trong `python-sidecar/app/utils/history_manager.py` để quản lý việc lưu trữ file âm thanh và metadata JSON.
    - Path lưu trữ: `data/voice_studio_history/` (relative to root).
    - Metadata schema: `{ "id": string, "filename": string, "text": string, "engine": string, "config": dict, "created_at": iso_date, "type": "tts" | "clone" | "design" }`.
    - Methods:
      - `save_history(file_content: bytes, metadata: dict) -> dict`: Lưu file audio và cập nhật file `history.json` index.
      - `get_all() -> list`: Đọc và trả về danh sách từ `history.json`.
      - `delete_history(id: string)`: Xóa file audio và entry trong JSON.
    - Đảm bảo tạo thư mục `data/voice_studio_history` nếu chưa tồn tại.
  </action>
  <verify>
    <automated>python -c "from app.utils.history_manager import HistoryManager; hm = HistoryManager(); print('HistoryManager initialized')"</automated>
  </verify>
  <done>HistoryManager class implemented with save/get/delete capabilities.</done>
  <acceptance_criteria>
    - `python-sidecar/app/utils/history_manager.py` contains `class HistoryManager`
    - Method `save_history` creates files in `data/voice_studio_history/`
    - Method `get_all` returns a list of history items
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Voice Studio API Routes</name>
  <files>python-sidecar/app/routes/voice_studio.py, python-sidecar/app/main.py</files>
  <read_first>
    - python-sidecar/app/routes/tts_profiles.py
    - python-sidecar/app/main.py
  </read_first>
  <action>
    Tạo các endpoints API cho Voice Studio:
    1. Tạo `python-sidecar/app/routes/voice_studio.py`:
       - `GET /api/voice-studio/history`: Trả về danh sách history từ `HistoryManager`.
       - `DELETE /api/voice-studio/history/{id}`: Xóa một item.
       - `POST /api/voice-studio/generate-tts`: Nhận text + config, gọi TTS engine hiện có, lưu qua `HistoryManager`, trả về metadata.
    2. Đăng ký router mới trong `python-sidecar/app/main.py`:
       - `from app.routes import voice_studio`
       - `app.include_router(voice_studio.router, prefix="/api/voice-studio", tags=["voice-studio"])`
  </action>
  <verify>
    <automated>grep -q "voice_studio" python-sidecar/app/main.py</automated>
  </verify>
  <done>Endpoints registered and accessible via Sidecar API.</done>
  <acceptance_criteria>
    - `python-sidecar/app/routes/voice_studio.py` exists with router definitions
    - `python-sidecar/app/main.py` includes the `voice_studio` router
    - `GET /api/voice-studio/history` endpoint is defined
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Frontend Voice Studio Store</name>
  <files>src/stores/useVoiceStudioStore.ts</files>
  <read_first>
    - src/stores/useProjectStore.ts
    - .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-PATTERNS.md
  </read_first>
  <action>
    Tạo Zustand store `useVoiceStudioStore` để quản lý trạng thái của Voice Studio Tool.
    - State: `history: HistoryItem[]`, `loading: boolean`, `activeTab: 'tts' | 'clone' | 'design'`.
    - Actions:
      - `fetchHistory()`: Gọi `GET /api/voice-studio/history`.
      - `generateTTS(text, config)`: Gọi `POST /api/voice-studio/generate-tts` và refresh history.
      - `deleteHistory(id)`: Gọi `DELETE /api/voice-studio/history/{id}`.
      - `setActiveTab(tab)`.
    - Types: Định nghĩa `HistoryItem` interface khớp với backend metadata.
  </action>
  <verify>
    <automated>powershell -Command "Select-String 'useVoiceStudioStore' src/stores/useVoiceStudioStore.ts"</automated>
  </verify>
  <done>Zustand store implemented and exported.</done>
  <acceptance_criteria>
    - `src/stores/useVoiceStudioStore.ts` exports `useVoiceStudioStore`
    - Store contains `fetchHistory`, `generateTTS`, and `deleteHistory` actions
    - `HistoryItem` interface is defined
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Voice Studio UI → Sidecar API | Requests for generation and history management |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | HistoryManager | mitigate | Sử dụng `path_utils` để sanitize tên file, ngăn chặn path traversal. |
| T-11-02 | Info Disclosure | history.json | mitigate | Đảm bảo file JSON index không chứa sensitive info và chỉ accessible qua local API. |
| T-11-03 | Denial of Service | generate-tts | mitigate | Giới hạn độ dài text đầu vào (ví dụ 5000 chars) để tránh treo hệ thống. |
</threat_model>

<verification>
- Khởi động Sidecar, gọi `GET /api/voice-studio/history` qua curl/Postman, nhận kết quả empty array (vì mới setup).
- Kiểm tra store được khởi tạo đúng trong project.
</verification>

<success_criteria>
- File `python-sidecar/data/voice_studio_history/history.json` được tạo tự động khi cần.
- API endpoints hoạt động và trả về data đúng schema.
- Store đồng bộ được dữ liệu từ API.
</success_criteria>

<output>
After completion, create .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-01-SUMMARY.md
</output>
