---
phase: 11-voice-studio-full-features-text-to-speech-audio-history-ui-u
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/components/tools/voicestudio/TTSTab.tsx
  - src/components/tools/voicestudio/HistoryPanel.tsx
  - src/components/tools/voicestudio/CloneTab.tsx
  - src/components/tools/voicestudio/DesignTab.tsx
autonomous: true
requirements:
  - M3-VS-01
  - M3-VS-02
  - M3-VS-03
  - M3-VS-04
  - M3-VS-06
must_haves:
  truths:
    - "Người dùng có thể tạo audio từ text sử dụng Edge, ElevenLabs hoặc OmniVoice"
    - "Danh sách lịch sử hiển thị đầy đủ các audio đã tạo kèm trình phát nhạc"
    - "Cấu hình Speed/Pitch/Vol hoạt động chính xác cho TTS"
  artifacts:
    - path: "src/components/tools/voicestudio/TTSTab.tsx"
      provides: "Full TTS generation interface"
    - path: "src/components/tools/voicestudio/HistoryPanel.tsx"
      provides: "Reusable history list with audio playback"
  key_links:
    - from: "TTSTab.tsx"
      to: "useVoiceStudioStore.generateTTS"
      via: "form submission"
---

<objective>
Hoàn thiện các tính năng cốt lõi của Voice Studio: Tab Text-to-Speech hỗ trợ đa engine và Panel quản lý lịch sử âm thanh. Tích hợp lịch sử vào tất cả các tab để người dùng dễ dàng quản lý thành phẩm.

Purpose: Thực thi các quyết định D-01, D-02, D-03 và D-06 về chức năng và layout.
Output: Tab TTS hoàn chỉnh và hệ thống History đồng bộ.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-CONTEXT.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-RESEARCH.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-PATTERNS.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-01-SUMMARY.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-02-SUMMARY.md

<interfaces>
From src/components/controls/SelectControl.tsx:
```typescript
export interface SelectOption { value: string; label: string; }
export function SelectControl(props: { label: string; value: string; options: SelectOption[]; onChange: (v: string) => void; }) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Text-to-Speech Tab</name>
  <files>src/components/tools/voicestudio/TTSTab.tsx</files>
  <read_first>
    - src/components/properties/TextTab.tsx
    - .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-PATTERNS.md
  </read_first>
  <action>
    Xây dựng giao diện Tab TTS trong `src/components/tools/voicestudio/TTSTab.tsx`:
    1. Phần Input: Sử dụng `textarea` lớn cho nội dung văn bản.
    2. Phần Settings (D-01, D-02):
       - `SelectControl` cho Engine: Edge, ElevenLabs, OmniVoice.
       - `SelectControl` cho Voice (tùy thuộc engine được chọn).
       - `SliderControl` cho Speed (0.5x - 2.0x), Volume (0 - 100), và Pitch (-10 to 10).
    3. Nút "Generate Audio":
       - Gọi `generateTTS` từ `useVoiceStudioStore`.
       - Hiển thị loading state khi đang xử lý.
    4. Layout: Đặt các controls này vào phần "Creation zone" (phía trên).
  </action>
  <verify>
    <automated>grep -q "textarea" src/components/tools/voicestudio/TTSTab.tsx</automated>
  </verify>
  <done>TTSTab implemented with multi-engine support and config sliders.</done>
  <acceptance_criteria>
    - `TTSTab.tsx` contains engine selection for Edge, ElevenLabs, and OmniVoice
    - Sliders for Speed, Volume, and Pitch are present
    - `generateTTS` is called on button click
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement and Integrate History Panel</name>
  <files>src/components/tools/voicestudio/HistoryPanel.tsx, src/components/tools/voicestudio/TTSTab.tsx, src/components/tools/voicestudio/CloneTab.tsx, src/components/tools/voicestudio/DesignTab.tsx</files>
  <read_first>
    - src/components/tools/VoiceCloneWindow.tsx (Logic phát nhạc)
    - src/stores/useVoiceStudioStore.ts
  </read_first>
  <action>
    1. Tạo `src/components/tools/voicestudio/HistoryPanel.tsx`:
       - Nhận `type: 'tts' | 'clone' | 'design'` làm props để filter danh sách history.
       - Lấy `history` từ `useVoiceStudioStore`.
       - Render danh sách card: {Text/Name, Engine, Date, Action Buttons}.
       - Action Buttons: `Play/Pause`, `Open Folder` (mở local file), `Delete`.
       - Tích hợp audio player logic (Dùng chung 1 `audio` element hoặc quản lý `playingId` để tránh phát chồng chéo).
    2. Nhúng `HistoryPanel` vào cuối mỗi tab (TTSTab, CloneTab, DesignTab) theo layout D-06.
    3. Gọi `fetchHistory()` trong `useEffect` tại `VoiceStudioWindow` hoặc từng Tab.
  </action>
  <verify>
    <automated>powershell -Command "Select-String 'HistoryPanel' src/components/tools/voicestudio/TTSTab.tsx"</automated>
  </verify>
  <done>HistoryPanel implemented and integrated into all tabs.</done>
  <acceptance_criteria>
    - `HistoryPanel.tsx` exists and displays history items
    - History items can be played and deleted
    - Every tab (TTS, Clone, Design) has a history section at the bottom
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| History Panel → File System | Opening local audio files |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Info Disclosure | HistoryPanel | accept | Việc hiển thị metadata của file audio local là an toàn trong phạm vi ứng dụng cá nhân. |
</threat_model>

<verification>
- Chọn Tab TTS, nhập text, chọn Edge TTS, nhấn Generate -> File xuất hiện ở History bên dưới.
- Nhấn Play ở history -> Audio phát đúng.
- Chuyển sang Tab Clone -> History chỉ hiện các item loại 'clone' (nếu có logic filter).
</verification>

<success_criteria>
- TTS hoạt động with ít nhất 2 engine (Edge và OmniVoice).
- Lịch sử được lưu và hiển thị ngay lập tức sau khi tạo.
- Giao diện đúng cấu trúc Tạo mới / Lịch sử.
</success_criteria>

<output>
After completion, create .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-03-SUMMARY.md
</output>
