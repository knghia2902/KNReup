---
phase: 11-voice-studio-full-features-text-to-speech-audio-history-ui-u
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/tools/VoiceStudioWindow.tsx
  - src/components/tools/voicestudio/CloneTab.tsx
  - src/components/tools/voicestudio/DesignTab.tsx
  - src/utils/windowManager.ts
  - src/components/setup/HomeLauncher.tsx
  - src-tauri/tauri.conf.json
autonomous: true
requirements:
  - M3-VS-05
must_haves:
  truths:
    - "Cửa sổ công cụ được đổi tên thành Voice Studio trên Launcher và Titlebar"
    - "Giao diện sử dụng Tab ngang thay vì Pill tabs"
    - "Tính năng Voice Clone và Voice Design hiện tại vẫn hoạt động sau khi migrate"
  artifacts:
    - path: "src/components/tools/VoiceStudioWindow.tsx"
      provides: "Main shell for Voice Studio with horizontal tabs"
    - path: "src/components/tools/voicestudio/CloneTab.tsx"
      provides: "Voice Clone feature logic"
    - path: "src/components/setup/HomeLauncher.tsx"
      provides: "Updated entry point for Voice Studio"
  key_links:
    - from: "src/utils/windowManager.ts"
      to: "VoiceStudioWindow.tsx"
      via: "window label/component mapping"
---

<objective>
Tái cấu trúc giao diện từ "Voice Clone" sang "Voice Studio" với hệ thống Tab ngang chuyên nghiệp. Di chuyển các logic Clone/Design hiện có vào các component con để chuẩn bị cho việc tích hợp TTS và History.

Purpose: Nâng cấp trải nghiệm người dùng và thương hiệu công cụ theo quyết định D-05.
Output: Shell UI mới, các tab đã migrate và entry point cập nhật.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-CONTEXT.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-PATTERNS.md
@.planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-01-SUMMARY.md

<interfaces>
From src/components/tools/VoiceCloneWindow.tsx (Current State):
```typescript
export function VoiceCloneWindow() {
  const [activeTab, setActiveTab] = useState<'clone' | 'design'>('clone');
  // ... logic to be moved ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename and Refactor Voice Studio Shell</name>
  <files>src/components/tools/VoiceStudioWindow.tsx, src/components/tools/VoiceCloneWindow.tsx</files>
  <read_first>
    - src/components/tools/VoiceCloneWindow.tsx
    - .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-PATTERNS.md
  </read_first>
  <action>
    1. Rename file `src/components/tools/VoiceCloneWindow.tsx` thành `src/components/tools/VoiceStudioWindow.tsx`.
    2. Cập nhật tên Component thành `VoiceStudioWindow`.
    3. Thay đổi giao diện chọn Tab từ dạng Pill sang Horizontal Tabs (D-05):
       - Sử dụng `activeTab` từ `useVoiceStudioStore`.
       - Render 3 tabs: "Text-to-Speech" (mặc định), "Voice Clone", "Voice Design".
       - UI: Icons từ Phosphor (`Microphone`, `MagicWand`, `Waveform`).
    4. Giữ lại phần `useSidecar` and `useTheme` hooks.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path src/components/tools/VoiceStudioWindow.tsx"</automated>
  </verify>
  <done>File renamed and shell structure updated to 3 tabs.</done>
  <acceptance_criteria>
    - `src/components/tools/VoiceStudioWindow.tsx` exists
    - Component name is `VoiceStudioWindow`
    - Tabs "Text-to-Speech", "Voice Clone", "Voice Design" are present in code
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate Clone and Design Logic to Tabs</name>
  <files>src/components/tools/voicestudio/CloneTab.tsx, src/components/tools/voicestudio/DesignTab.tsx</files>
  <read_first>
    - src/components/tools/VoiceStudioWindow.tsx
    - src/components/tools/VoiceCloneWindow.tsx (Original)
  </read_first>
  <action>
    Tạo 2 component mới `CloneTab.tsx` và `DesignTab.tsx` trong thư mục `src/components/tools/voicestudio/`.
    - Di chuyển logic form và xử lý API tương ứng từ `VoiceCloneWindow` cũ vào các component này.
    - Props: Kế thừa các states cần thiết nếu có (ví dụ `playingId`).
    - Đảm bảo layout phần "Creation" ở phía trên.
    - Import và render chúng trong `VoiceStudioWindow.tsx` dựa trên `activeTab`.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path src/components/tools/voicestudio/CloneTab.tsx"</automated>
  </verify>
  <done>Clone and Design features migrated to dedicated components.</done>
  <acceptance_criteria>
    - `src/components/tools/voicestudio/CloneTab.tsx` and `DesignTab.tsx` exist
    - Logic for cloning and designing is moved out of the main shell
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update Launcher and Window Management</name>
  <files>src/utils/windowManager.ts, src/components/setup/HomeLauncher.tsx, src-tauri/tauri.conf.json</files>
  <read_first>
    - src/utils/windowManager.ts
    - src/components/setup/HomeLauncher.tsx
    - src-tauri/tauri.conf.json
  </read_first>
  <action>
    Đồng bộ hóa tên gọi và định danh mới cho công cụ:
    1. Trong `src/utils/windowManager.ts`:
       - Đổi `tool-voice-clone` thành `tool-voice-studio`.
       - Cập nhật label cửa sổ thành "Voice Studio".
    2. Trong `src/components/setup/HomeLauncher.tsx`:
       - Đổi Label "Voice Clone" thành "Voice Studio".
       - Cập nhật Icon thành `Waveform` (hoặc icon phù hợp từ Research).
       - Cập nhật hàm gọi `openTool('voice-studio')`.
    3. Trong `src-tauri/tauri.conf.json`:
       - Cập nhật định danh window label `tool-voice-clone` thành `tool-voice-studio` (nếu có cấu hình tĩnh).
  </action>
  <verify>
    <automated>grep -q "Voice Studio" src/components/setup/HomeLauncher.tsx</automated>
  </verify>
  <done>Launcher and Tauri configuration updated to reflect "Voice Studio".</done>
  <acceptance_criteria>
    - `src/components/setup/HomeLauncher.tsx` contains "Voice Studio" text
    - `src/utils/windowManager.ts` uses `voice-studio` identifier
    - Window title is updated to "Voice Studio"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Window Manager | Invoking new tool windows |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Window Manager | accept | Rủi ro thấp vì định danh window được kiểm soát nội bộ. |
</threat_model>

<verification>
- Mở Home Launcher, xác nhận icon/label đã đổi.
- Click mở Voice Studio, xác nhận cửa sổ mở đúng và hiển thị 3 tab.
- Thử nghiệm tính năng Clone hiện có để đảm bảo không có regression.
</verification>

<success_criteria>
- Home Launcher hiển thị "Voice Studio".
- Voice Studio Window hiển thị với layout tab ngang mới.
- Các tính năng cũ (Clone/Design) hoạt động bình thường trong tab mới.
</success_criteria>

<output>
After completion, create .planning/phases/11-voice-studio-full-features-text-to-speech-audio-history-ui-u/11-02-SUMMARY.md
</output>
