---
description: "Wave 4: State Management & Pipeline Integration"
wave: 4
depends_on: ["03-02-PLAN.md", "03-03-PLAN.md"]
files_modified: 
  - "src/stores/useProjectStore.ts"
  - "src/stores/useSubtitleStore.ts"
  - "src/components/editor/PropertiesPanel.tsx"
  - "src/components/layout/Sidebar.tsx"
autonomous: true
---

# Phase 3, Plan 4: State Management & Pipeline Integration

<objective>
Ràng buộc logic tương tác (Zustand & State) vào toàn bộ hệ thống UI mới. Nối các Properties Controls với Zustand Store, thiết lập Sidebar view logic, và tính năng xử lý Pipeline / SRT API nội bộ.
</objective>

<requirements>
- Sidebar state switching (Preview/Subtitle/Pipeline/Monitor focus)
- Drag & drop video import
- SRT import/export
- Properties tabs control bindings
</requirements>

<tasks>
  <task>
    <read_first>
      - src/stores/useProjectStore.ts
      - src/stores/useSubtitleStore.ts
      - src/components/editor/PropertiesPanel.tsx
    </read_first>
    <action>
      1. Khai báo schema Zustand cho 4 nhóm (STYLE, TTS, SUB, OUT) như trong tài liệu `03-RESEARCH.md`.
      2. Tại `PropertiesPanel.tsx` (và các child components tạo ở Wave 2), móc nối giá trị các ô Input/Select/Slider vào Zustand store thay vì fix cứng giao diện. Ràng buộc `onChange` để cập nhật Store.
    </action>
    <acceptance_criteria>
      - Changing a dropdown value in the UI correctly updates `useProjectStore` state
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - src/App.tsx
      - src/components/layout/Sidebar.tsx
      - src/components/layout/Titlebar.tsx
    </read_first>
    <action>
      1. Khai báo state quản lý View (`workspace`: editor/downloader; `view`: preview/subtitle/pipeline).
      2. Highlight CSS `.active` trên các thẻ `div.tb-tab` và `div.sb-ico` dựa vào state.
      3. Gắn onClick handlers vào Sidebar để luân chuyển trạng thái các panel (chẳng hạn ẩn Timeline khi sang màn Monitor).
    </action>
    <acceptance_criteria>
      - Clicking a Sidebar icon highlights it with `.active`
      - Clicking Titlebar tab updates workspace state
    </acceptance_criteria>
  </task>
</tasks>

<verification>
### Manual UI Check
1. Giao diện thay đổi Sidebar Icon mượt mà khi click.
2. Dữ liệu thay đổi trên Properties Panel (STYLE/TTS) phải lưu giữ và đồng bộ khi switch tab.
</verification>

<must_haves>
- State Management hoàn thiện cho các thao tác căn bản. Layout mượt mà.
</must_haves>
