---
description: "Wave 2: Properties Panel, Media Bin & Controls"
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified: 
  - "src/components/layout/NLELayout.css"
  - "src/components/editor/MediaBin.tsx"
  - "src/components/editor/PropertiesPanel.tsx"
  - "src/components/controls/*"
autonomous: true
---

# Phase 3, Plan 2: Properties Panel, Media Bin & Controls

<objective>
Xây dựng hệ thống UI Controls cực kỳ tinh xảo cho khu vực Media Bin (`.mbin`) và Properties Panel (`.pp`). Giao diện chia thành 4 tabs (STYLE, TTS, SUB, OUT) với toàn bộ Dropdown, Input, Slider, Toggle bám sát 100% thiết kế HTML mẫu. 
</objective>

<requirements>
- Properties tab STYLE (5 sections)
- Properties tab SUB (segment list)
- Properties tab TTS (voice configs)
- Properties tab OUT (codec, export queue)
- Media Bin (danh sách tệp mp4)
</requirements>

<tasks>
  <task>
    <read_first>
      - d:\Tools\KNReup\knreup-complete.html
      - src/components/layout/NLELayout.css
    </read_first>
    <action>
      1. Copy toàn bộ subset CSS liên quan đến `.pp`, `.ptabs`, `.pbody`, `.ps`, `.pshd`, `.pr`, inputs (`.psel`, `.pinp`, `.ptxt`, `.pnum`, `.pslider`, `.tog`, `.cinp`, v.v...) từ HTML mẫu vào `NLELayout.css` (hoặc `properties.css`).
      2. Mở rộng hệ thống Control Component dùng chung:
         - Tạo `ToggleControl.tsx` bám theo DOM `.tog.on > .togthumb`
         - Tạo `SelectControl.tsx` bám theo DOM `.psel`
         - Tạo `NumberControl.tsx` bám theo DOM `.pnum`
         - Tạo `ChipGroup.tsx` bám theo DOM `.chiprow > .chip`
    </action>
    <acceptance_criteria>
      - `ToggleControl` is exported and functional UI-wise
      - CSS rules for `.psel`, `.tog`, `.pnum` exist in project
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - d:\Tools\KNReup\knreup-complete.html
      - src/components/editor/PropertiesPanel.tsx
    </read_first>
    <action>
      1. Đập đi xây lại `PropertiesPanel.tsx`. 
      2. Cấu trúc HTML chính xác:
         - `<div className="pp">`
         - `<div className="ptabs">` chứa 4 `<div className="pt">` (STYLE, TTS, SUB, OUT)
         - Logic React state `activeTab` để switch hiển thị class `.tc.vis`.
      3. Dựng code tĩnh (static JSX) cho 4 tab này sao cho y hệt nội dung mockup HTML của sếp (khoảng dòng 300→530 của knreup-complete.html). 
          - Tab SUB chứa danh sách subtitle `.slist` > `.srow`
          - Tab STYLE chứa các color picker `.cinp`
    </action>
    <acceptance_criteria>
      - `PropertiesPanel.tsx` defines 4 visually authentic tabs that can be clicked to switch
      - DOM tree matches exactly (e.g., uses `.pshd` for section headers)
    </acceptance_criteria>
  </task>

  <task>
    <read_first>
      - d:\Tools\KNReup\knreup-complete.html
      - src/components/editor/MediaBin.tsx
    </read_first>
    <action>
      1. Tạo `MediaBin.tsx` render khu vực `.mbin` (bên trái Video Preview).
      2. Dựng `.phd` (Media count) + `.mlist` (chứa `.mitem` thumbnail thẻ video + `.dropz` khu vực import).
      3. Nhúng component này vào `NLELayout.tsx`.
    </action>
    <acceptance_criteria>
      - `MediaBin` exports correctly
      - Render matches HTML classes `.mitem`, `.mthumb`, `.minfo`
    </acceptance_criteria>
  </task>
</tasks>

<verification>
### Manual UI Check
1. Chạy App. Panel bên phải (264px) có 4 tabs (STYLE, TTS, SUB, OUT).
2. Click qua lại giữa các tab phải chuyển nội dung.
3. Panel bên trái (204px) hiển thị giao diện chứa thumbnail danh sách video Media Bin và nút Drop to import.
</verification>

<must_haves>
- Không lệch một class CSS nào so với HTML cung cấp. Text size 10px, 9px monospace Cockpit style phải hiển thị sắc nét.
</must_haves>
