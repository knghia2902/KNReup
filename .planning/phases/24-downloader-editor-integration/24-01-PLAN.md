---
phase: 24-downloader-editor-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  python-sidecar/app/engines/downloader/database.py,
  python-sidecar/app/engines/downloader/manager.py,
  python-sidecar/app/routes/downloader.py,
  src/stores/useLauncherStore.ts,
  src/hooks/useDownloader.ts,
  src/components/downloader/DownloaderPanel.tsx
]
autonomous: true
requirements: [INT-01, INT-02]

must_haves:
  truths:
    - "Downloader database supports project_id column"
    - "Downloader API accepts project_id and saves files in project-specific folders"
    - "Downloader UI allows selecting active project context"
  artifacts:
    - path: "python-sidecar/app/engines/downloader/database.py"
      provides: "Updated schema with project_id"
    - path: "src/stores/useLauncherStore.ts"
      provides: "activeProjectId state"
  key_links:
    - from: "src/components/downloader/DownloaderPanel.tsx"
      to: "python-sidecar/app/routes/downloader.py"
      via: "POST /api/download/start with project_id"
---

<objective>
Implement the core infrastructure for Project-Centric downloading, linking downloads to specific projects at the database and file system levels.

Purpose: Ensure media is organized by project from the moment it is downloaded.
Output: Project-aware backend and downloader UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-downloader-editor-integration/RESEARCH.md
@python-sidecar/app/engines/downloader/database.py
@python-sidecar/app/engines/downloader/manager.py
@python-sidecar/app/routes/downloader.py
@src/stores/useLauncherStore.ts
@src/hooks/useDownloader.ts
@src/components/downloader/DownloaderPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Database & Route Migration</name>
  <files>
    python-sidecar/app/engines/downloader/database.py,
    python-sidecar/app/routes/downloader.py
  </files>
  <action>
    1. Update `python-sidecar/app/engines/downloader/database.py`:
       - In `init_db`, add logic to add `project_id TEXT` column to `downloads` table if it doesn't exist (use `PRAGMA table_info(downloads)`).
       - Update `add_download` to accept `project_id: Optional[str] = None` and insert it.
       - Update `list_downloads` to filter by `project_id` if provided.
    2. Update `python-sidecar/app/routes/downloader.py`:
       - Update `StartDownloadRequest` model to include `project_id: str = ""` and `project_name: str = ""`.
       - Pass these to `manager.start_download`.
       - Update `get_history` route to accept `project_id: Optional[str] = None` query param and pass to manager.
  </action>
  <verify>
    <automated>python -c "import sqlite3; conn = sqlite3.connect('python-sidecar/data/downloads.db'); print('project_id' in [i[1] for i in conn.execute('PRAGMA table_info(downloads)')])"</automated>
  </verify>
  <done>Backend DB and Routes support project_id.</done>
</task>

<task type="auto">
  <name>Task 2: Project-Aware Download Manager</name>
  <files>python-sidecar/app/engines/downloader/manager.py</files>
  <action>
    1. Update `start_download` signature to include `project_id` and `project_name`.
    2. Pass `project_id` to `add_download` (or `update_download` if existing).
    3. In `_download_worker`, if `project_name` is provided:
       - Construct `output_dir` as `DEFAULT_OUTPUT_DIR / sanitized_project_name / platform`.
       - Use `pathlib` or simple slugification to ensure project name is a safe folder name.
  </action>
  <verify>
    <automated>grep "project_id" python-sidecar/app/engines/downloader/manager.py</automated>
  </verify>
  <done>DownloadManager organizes files by project name.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend Store & Downloader Hook Update</name>
  <files>src/stores/useLauncherStore.ts, src/hooks/useDownloader.ts</files>
  <action>
    1. Update `src/stores/useLauncherStore.ts`:
       - Add `activeProjectId: string | null` to `LauncherState`.
       - Add `setActiveProjectId: (id: string | null) => void`.
    2. Update `src/hooks/useDownloader.ts`:
       - Update `startDownload` to accept `projectId: string` and `projectName?: string`.
       - Send these in the POST body to `/api/download/start`.
       - Update `fetchHistory` to accept an optional `projectId`.
  </action>
  <verify>
    <automated>grep "activeProjectId" src/stores/useLauncherStore.ts</automated>
  </verify>
  <done>Frontend state and hook support project context.</done>
</task>

<task type="auto">
  <name>Task 4: Downloader UI Project Selector</name>
  <files>src/components/downloader/DownloaderPanel.tsx</files>
  <action>
    1. Import `useLauncherStore`.
    2. Add a `ProjectSelector` component or simple `<select>` in `DownloaderPanel` (near `URLInput`).
    3. Initialize with `activeProjectId` from store.
    4. When starting a download via `handleDownload`, pass the selected project's ID and name to `startDownload`.
  </action>
  <verify>
    <automated>grep "useLauncherStore" src/components/downloader/DownloaderPanel.tsx</automated>
  </verify>
  <done>Users can select which project to download into from the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Sidecar API | project_id and project_name passed as strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | manager.py | mitigate | Sanitize project_name before creating directories to prevent path traversal. |
</threat_model>

<verification>
1. Select a project in the Downloader panel.
2. Download a video.
3. Verify in file system that the video is saved in a subfolder named after the project.
4. Verify in SQLite (e.g. via `sqlite3`) that the `downloads` table has the correct `project_id`.
</verification>

<success_criteria>
- Downloads are successfully categorized by project in the DB and file system.
- UI reflects the active project context.
</success_criteria>

<output>
After completion, create `.planning/phases/24-downloader-editor-integration/24-01-SUMMARY.md`
</output>
