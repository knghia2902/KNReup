---
phase: 24-downloader-editor-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hooks/useDownloader.ts, src/components/downloader/DownloaderPanel.tsx, src/components/downloader/DownloadHistory.tsx]
autonomous: true
requirements: [INT-01, INT-02]

must_haves:
  truths:
    - "DownloadItem interface includes file_path property"
    - "Completed downloads in history show a 'Send to Editor' button"
    - "Clicking 'Send to Editor' creates a project in Launcher and opens a new Editor window"
  artifacts:
    - path: "src/hooks/useDownloader.ts"
      provides: "Updated DownloadItem interface"
    - path: "src/components/downloader/DownloaderPanel.tsx"
      provides: "Integration logic between Downloader and Launcher/Editor"
    - path: "src/components/downloader/DownloadHistory.tsx"
      provides: "UI button for Editor integration"
  key_links:
    - from: "src/components/downloader/DownloaderPanel.tsx"
      to: "src/stores/useLauncherStore.ts"
      via: "addProject call"
    - from: "src/components/downloader/DownloaderPanel.tsx"
      to: "src/utils/windowManager.ts"
      via: "openEditor call"
---

<objective>
Integrate the Downloader module with the NLE Editor to allow users to instantly start editing downloaded videos.

Purpose: Streamline the workflow from downloading content to editing it.
Output: A functional "Send to Editor" feature in the Downloader history.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-downloader-editor-integration/RESEARCH.md
@src/hooks/useDownloader.ts
@src/components/downloader/DownloaderPanel.tsx
@src/components/downloader/DownloadHistory.tsx
@src/stores/useLauncherStore.ts
@src/utils/windowManager.ts
</context>

<tasks>

<task type="auto">
  <name>Update DownloadItem Interface</name>
  <files>src/hooks/useDownloader.ts</files>
  <action>
    Add `file_path?: string` to the `DownloadItem` interface in `src/hooks/useDownloader.ts`. 
    This field is returned by the backend but was missing from the frontend type definition.
  </action>
  <verify>
    <automated>grep "file_path?: string" src/hooks/useDownloader.ts</automated>
  </verify>
  <done>DownloadItem interface contains the file_path property.</done>
</task>

<task type="auto">
  <name>Implement Integration Logic in DownloaderPanel</name>
  <files>src/components/downloader/DownloaderPanel.tsx</files>
  <action>
    1. Import `useLauncherStore` from `../../stores/useLauncherStore`.
    2. Import `openEditor` from `../../utils/windowManager`.
    3. Implement `handleSendToEditor` function:
       - Verify `item.file_path` exists.
       - Generate a `projectId` (e.g., `proj-${Date.now()}`).
       - Call `useLauncherStore.getState().addProject` with metadata:
         - `id`: projectId
         - `name`: `item.title`
         - `path`: ""
         - `videoPath`: `item.file_path`
         - `filePaths`: `[item.file_path]`
         - `createdAt`, `lastModified`: `Date.now()`
       - Call `await openEditor(projectId)`.
    4. Pass `handleSendToEditor` as `onSendToEditor` prop to the `DownloadHistory` component.
  </action>
  <verify>
    <automated>grep "handleSendToEditor" src/components/downloader/DownloaderPanel.tsx</automated>
  </verify>
  <done>DownloaderPanel implements and passes the Send to Editor logic.</done>
</task>

<task type="auto">
  <name>Add Send to Editor Button to UI</name>
  <files>src/components/downloader/DownloadHistory.tsx</files>
  <action>
    1. Update `DownloadHistoryProps` to include `onSendToEditor?: (item: DownloadItem) => void`.
    2. Import `MagicWand` from `@phosphor-icons/react`.
    3. In the actions section of the download row, add the button:
       - Only show if `item.status === 'completed'` and `!missingFiles[item.id]`.
       - Use `MagicWand` icon (size 18, weight bold).
       - Add title "Dựng video này".
       - On click, call `onSendToEditor?.(item)`.
    4. Place it before the `FolderOpen` button.
  </action>
  <verify>
    <automated>grep "MagicWand" src/components/downloader/DownloadHistory.tsx</automated>
  </verify>
  <done>The button is visible and functional in the download history list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Downloader -> Launcher Store | Internal state update via shared localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | src/stores/useLauncherStore.ts | mitigate | Project paths stored in local localStorage, standard for desktop apps. |
| T-24-02 | Tampering | src/utils/windowManager.ts | mitigate | openEditor validates projectId before spawning window. |
</threat_model>

<verification>
1. Download a video in the Downloader module.
2. Verify 'Send to Editor' (MagicWand) button appears in history.
3. Click the button and confirm a new Editor window opens.
4. Verify the video is correctly loaded into the Editor (check Media Bin or Preview).
</verification>

<success_criteria>
- Users can transfer downloaded videos to the Editor with a single click.
- Projects are correctly registered in the Launcher/Home screen after transfer.
- No regression in existing download/history functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/24-downloader-editor-integration/24-01-SUMMARY.md`
</output>
