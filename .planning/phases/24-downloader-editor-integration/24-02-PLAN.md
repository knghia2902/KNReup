---
phase: 24-downloader-editor-integration
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [
  src/App.tsx,
  src/components/setup/RecentProjects.tsx,
  src/utils/windowManager.ts
]
autonomous: true
requirements: [INT-01, INT-02]

must_haves:
  truths:
    - "Editor auto-loads completed downloads linked to the current project"
    - "Home Launcher project cards have buttons to open Downloader or Editor"
    - "Opening Downloader from a project card sets that project as active"
  artifacts:
    - path: "src/App.tsx"
      provides: "Project-linked media auto-loading"
    - path: "src/components/setup/RecentProjects.tsx"
      provides: "Enhanced project cards"
  key_links:
    - from: "src/components/setup/RecentProjects.tsx"
      to: "src/utils/windowManager.ts"
      via: "openModule calls"
---

<objective>
Enhance the Project Hub experience by streamlining the transition between Home, Downloader, and Editor.

Purpose: Create a cohesive "Project-First" workflow where media is automatically discovered and modules are easily accessible.
Output: Integrated Launcher and Editor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-downloader-editor-integration/RESEARCH.md
@.planning/phases/24-downloader-editor-integration/24-01-SUMMARY.md
@src/App.tsx
@src/components/setup/RecentProjects.tsx
@src/utils/windowManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editor Auto-Load Project Media</name>
  <files>src/App.tsx</files>
  <action>
    1. In `App.tsx`, inside the `useEffect` that handles initialization (or a new one):
       - Check if `projectId` exists and sidecar is `connected`.
       - Call sidecar `/api/download/history?project_id=${projectId}&status=completed`.
       - For each download item, if its `file_path` is not already in `filePaths`:
         - Call `handleFileSelected(dl.file_path)`.
  </action>
  <verify>
    <automated>grep "project_id" src/App.tsx</automated>
  </verify>
  <done>Editor automatically loads downloads belonging to the project on startup.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced Home Launcher Project Cards</name>
  <files>src/components/setup/RecentProjects.tsx</files>
  <action>
    1. Update `ProjectCard` in `src/components/setup/RecentProjects.tsx`:
       - Add two buttons: "Mở Editor" and "Tải Video".
       - "Mở Editor" button: Calls `openEditor(project.id)`.
       - "Tải Video" button:
         - Calls `setActiveProjectId(project.id)`.
         - Calls `openModule('downloader')` (ensure `openModule` or similar exists in `windowManager`).
    2. Improve layout of project card to accommodate these actions without cluttering.
  </action>
  <verify>
    <automated>grep "openModule" src/components/setup/RecentProjects.tsx</automated>
  </verify>
  <done>Home Launcher project cards allow direct navigation to specific project modules.</done>
</task>

<task type="auto">
  <name>Task 3: Window Manager Multi-Module Support</name>
  <files>src/utils/windowManager.ts</files>
  <action>
    Ensure `windowManager.ts` supports opening the downloader module with a specific project context, or update it to handle generic module opening.
  </action>
  <verify>
    <automated>grep "export const openModule" src/utils/windowManager.ts || echo "Need to implement"</automated>
  </verify>
  <done>WindowManager supports navigating to different modules via Tauri windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launcher -> Window Manager | Module routing via URL parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Information Disclosure | src/App.tsx | mitigate | Only auto-loads files from local paths that match project_id in local DB. |
</threat_model>

<verification>
1. From Home Launcher, click "Tải Video" on a project.
2. Verify Downloader opens and the project is pre-selected.
3. Download a video.
4. Go back to Home Launcher, click "Mở Editor" on the same project.
5. Verify the downloaded video is already in the Media Bin/Library.
</verification>

<success_criteria>
- Seamless transition between Home, Downloader, and Editor for a specific project.
- No manual file importing required for project-linked downloads.
</success_criteria>

<output>
After completion, create `.planning/phases/24-downloader-editor-integration/24-02-SUMMARY.md`
</output>
