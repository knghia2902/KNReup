---
phase: 07.2-advanced-timeline-interactions
plan: 02
subsystem: editor
tags: [ui, timeline, refactor]
requires: [M3-ED-01, M3-ED-02]
provides: [HH:mm:ss format, BGM deletion, Toolbar refactor]
key-files: [src/components/editor/Timeline.tsx, src/components/editor/VideoControls.tsx]
metrics:
  duration: 20m
  completed_date: "2026-04-21"
---

# Phase 07.2 Plan 02: Refactor Timeline UI and Fix BGM Deletion Summary

## Substantive Changes
- Updated `formatTime` in `Timeline.tsx` and `VideoControls.tsx` to handle HH:mm:ss for durations over 1 hour.
- Refactored `Timeline.tsx` header to remove "TIMELINE" label and move toolbar (Scissors, ArrowLineLeft, ArrowLineRight, Trash) to the left.
- Added Trash icon to the toolbar with `handleDeleteSelected` method for BGM and subtitle deletion.
- Enhanced BGM deletion logic in `handleKeyDown` and integrated it with the UI.
- Improved toolbar layout and icon styling (18px size, spacing) for a cleaner, CapCut-like aesthetic.
- Fixed keyboard shortcut logic for Q, W, Ctrl+B, and Delete/Backspace keys.

## Key Decisions
- **BGM Deletion Unification:** Added `handleDeleteSelected` to unify deletion logic for both keyboard shortcuts and the Trash button.
- **Header Layout:** Moved the time display next to the toolbar icons on the left for better visibility and consistent design with modern NLEs.
- **Icon Set:** Standardized on `@phosphor-icons/react` for all timeline actions.

## Deviations from Plan
- None - plan executed exactly as written.

## Self-Check: PASSED
- [x] Time format changed to HH:mm:ss.
- [x] BGM can be deleted with Delete key and Trash icon.
- [x] Timeline header layout updated as requested.
- [x] All tasks committed individually.
