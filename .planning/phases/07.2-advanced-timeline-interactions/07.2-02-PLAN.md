---
phase: 07.2-advanced-timeline-interactions
plan: 07.2-02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/editor/Timeline.tsx, src/stores/useProjectStore.ts]
autonomous: true
requirements: [M3-ED-01]
must_haves:
  truths:
    - "Toolbar displays Split, Split Left, and Split Right buttons"
    - "Clicking Split on a subtitle clip divides it into two segments at the playhead"
    - "Clicking Split Left/Right on a clip trims it to the playhead position"
    - "Keyboard shortcuts Ctrl+B, Q, and W remain functional and sync with buttons"
  artifacts:
    - path: "src/components/editor/Timeline.tsx"
      provides: "Split toolbar UI"
    - path: "src/stores/useProjectStore.ts"
      provides: "Split/Trim logic for video/bgm"
---

<objective>
Implement "Split" functionality (Split Left, Split Right, Split at playhead) for the timeline editor with a CapCut-inspired toolbar.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07.2-advanced-timeline-interactions/07.2-CONTEXT.md
@src/components/editor/Timeline.tsx
@src/stores/useProjectStore.ts
@src/stores/useSubtitleStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Split/Trim logic to useProjectStore.ts</name>
  <files>src/stores/useProjectStore.ts</files>
  <action>
    - Add `splitLeft` and `splitRight` methods to `ProjectStore`.
    - These methods should adjust `vid_clip_start`, `vid_clip_duration` or `bgm_timeline_start`, `bgm_clip_duration` based on the current playhead time.
    - Note: Since the current store only supports a single video/bgm clip, "Split" (divide into two) will be deferred for video/bgm until multi-clip support is added in a later phase, but the UI should handle the request gracefully.
  </action>
  <verify>
    Check if methods exist and correctly update state values.
  </verify>
  <done>Store logic implemented for single-clip trimming.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Split Toolbar in Timeline.tsx</name>
  <files>src/components/editor/Timeline.tsx</files>
  <action>
    - Import `Scissors`, `ArrowLineLeft`, `ArrowLineRight` from `@phosphor-icons/react`.
    - Add a toolbar section in the `.tlhd` header.
    - Add three buttons: Split Left, Split, Split Right.
    - Connect buttons to:
      - Split: `useSubtitleStore.splitSegment` (if subtitle selected) or no-op/prepare for video.
      - Split Left: `Q` logic (integrated via store or event).
      - Split Right: `W` logic (integrated via store or event).
    - Style buttons to match CapCut: dark background, subtle borders, highlight on hover, disabled when no clip selected.
    - Remove the old floating `SplitButtons` component to avoid redundancy.
  </action>
  <verify>
    Visual check of the toolbar and functionality of buttons.
  </verify>
  <done>Timeline toolbar updated with Split functionality.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Styling and Consistency</name>
  <files>src/components/editor/Timeline.tsx, src/styles/design-system.css</files>
  <action>
    - Ensure all split actions provide visual feedback (e.g., clip resizing/splitting immediately).
    - Match the CapCut aesthetic: dark theme (`#18181c` or similar), red accent for the playhead, and clean icons.
    - Verify that keyboard shortcuts (Ctrl+B, Q, W) still work and use the new store methods if applicable.
  </action>
  <verify>
    Functional test of all 3 actions via buttons and shortcuts.
  </verify>
  <done>Split functionality polished and consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Store | State transitions for timeline clips |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-01 | Tampering | useProjectStore | mitigate | Validate clip start/duration bounds in store methods |
</threat_model>

<success_criteria>
- User can split a subtitle segment into two.
- User can trim a video/bgm clip from left or right using toolbar buttons.
- UI matches CapCut aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-advanced-timeline-interactions/07.2-02-SUMMARY.md`
</output>
