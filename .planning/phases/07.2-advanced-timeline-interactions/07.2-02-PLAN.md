---
phase: 07.2-advanced-timeline-interactions
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified: [src/components/editor/Timeline.tsx, src/stores/useProjectStore.ts, src/components/editor/VideoControls.tsx]
autonomous: true
requirements: [M3-ED-01, M3-ED-02]
must_haves:
  truths:
    - "Time display in Timeline and VideoControls shows HH:mm:ss format for durations"
    - "Pressing 'Delete' or 'Backspace' while BGM is selected removes the BGM from the project"
    - "Timeline header has toolbar icons (Split, Trash) at the start (left)"
    - "Redundant 'TIMELINE' text is removed from the header"
    - "Trash icon is available in the toolbar and deletes the selected subtitle or BGM"
  artifacts:
    - path: "src/components/editor/Timeline.tsx"
      provides: "Updated header layout and time formatting"
    - path: "src/stores/useProjectStore.ts"
      provides: "Split/Trim logic (existing) and BGM removal (via updateConfig)"
---

<objective>
Refactor the Timeline header for a cleaner, CapCut-like aesthetic, fix time display formatting inconsistencies, and implement BGM deletion.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/editor/Timeline.tsx
@src/stores/useProjectStore.ts
@src/components/editor/VideoControls.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Time Formatting and BGM Deletion Logic</name>
  <files>src/components/editor/Timeline.tsx, src/components/editor/VideoControls.tsx, src/stores/useProjectStore.ts</files>
  <action>
    - Update `formatTime` in `src/components/editor/Timeline.tsx`:
      - Change format from `HH:mm:ss:ff` to `HH:mm:ss`.
      - Ensure hours, minutes, and seconds are always 2-digit padded (e.g., `00:00:00`).
    - Update `formatTime` in `src/components/editor/VideoControls.tsx` to match the same padded `HH:mm:ss` format.
    - In `src/components/editor/Timeline.tsx`:
      - Update `handleKeyDown` to handle BGM deletion when `selectedClipId === 'bgm-main'`.
      - Action: `useProjectStore.getState().updateConfig({ bgm_enabled: false, bgm_file: '', selectedClipId: null })`.
      - Ensure `e.preventDefault()` is called to prevent page navigation.
  </action>
  <verify>
    - Check time display in UI for 00:00:00 format.
    - Select BGM track and press Delete; verify it disappears.
  </verify>
  <done>Time formatting fixed and BGM deletion implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Timeline Header Refactor (Layout & Icons)</name>
  <files>src/components/editor/Timeline.tsx</files>
  <action>
    - Refactor the `.tlhd` (Timeline Header) structure:
      - Remove the "TIMELINE" text label.
      - Create a toolbar container at the very start of the header.
      - Move Split buttons (`ArrowLineLeft`, `Scissors`, `ArrowLineRight`) into this toolbar.
      - Add a `Trash` icon button (from `@phosphor-icons/react`) to the toolbar.
      - Place the Time Display (`currentTime`) immediately after the toolbar icons.
      - Keep Zoom controls at the far right (`marginLeft: 'auto'`).
    - Update icon buttons to use Phosphor Icons consistently.
    - Style the toolbar:
      - Dark background for buttons, subtle borders, highlight on hover.
      - Ensure buttons are disabled when no appropriate clip is selected.
      - The `Trash` button should call the appropriate delete function based on `selectedClipId` (Subtitle delete or BGM remove).
  </action>
  <verify>
    - Visual check: Icons on the left, time display next to them, zoom on the right.
    - Functional check: Trash button removes selected items.
  </verify>
  <done>Timeline header refactored and icons updated.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Interactions and Verification</name>
  <files>src/components/editor/Timeline.tsx</files>
  <action>
    - Verify that all keyboard shortcuts (`Q`, `W`, `Ctrl+B`, `Delete`) are synchronized with the UI buttons.
    - Ensure visual feedback for selection (white border on clips) is clear.
    - Test edge cases: deleting when nothing is selected, splitting at the very beginning/end of a clip.
  </action>
  <verify>
    - All shortcuts and buttons work as expected.
    - No console errors during timeline interactions.
  </verify>
  <done>Timeline interactions polished and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Store | State updates for project configuration |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-02 | Tampering | useProjectStore | mitigate | Ensure store methods validate bounds and existing state |
</threat_model>

<success_criteria>
- Timeline header matches the requested layout (Toolbar Left, Time Center-ish, Zoom Right).
- Time is displayed as HH:mm:ss consistently.
- BGM can be deleted via Delete key or Trash icon.
- Split icons match CapCut aesthetic using Phosphor Icons.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-advanced-timeline-interactions/07.2-02-SUMMARY.md`
</output>
