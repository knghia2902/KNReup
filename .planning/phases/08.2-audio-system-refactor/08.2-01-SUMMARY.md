# Phase 08.2-01 SUMMARY — Audio System Refactor (Core)

## Status: ✅ Complete
Date: 2026-04-22

## Accomplishments
- Refactored all internal 'BGM' terminology to 'Audio' across frontend and backend.
- Updated `ProjectConfig` and `ProjectStore` in `src/stores/useProjectStore.ts` to use `audio_*` naming.
- Updated Sidecar `PipelineConfig` and `FFmpegOutputBuilder` to handle new `audio_*` parameters.
- Updated `usePipeline.ts` hook types for synchronization.
- Updated `TimelinePlaceholder.tsx` to display 'AUDIO' track label and use 'audio' CSS classes.
- Cleaned up `README.md` documentation to reflect the terminology change.
- Unified naming in `python-sidecar/app/engines/output.py` (variables like `a_music`, `audio_mix`).

## Verification Results
- `grep bgm` returns 0 matches in the entire codebase.
- Logic for `splitLeft` and `splitRight` in the store correctly handles `audio-main` clips.
- Sidecar rendering logic updated and compatible with the new field names.

## Next Step
Phase 08.2-02 — UI Renaming & Import Implementation.
- Update `AudioTab.tsx` and `Timeline.tsx` labels.
- Implement "Import Audio" button in `AudioLibrary.tsx` with Tauri's dialog API.
