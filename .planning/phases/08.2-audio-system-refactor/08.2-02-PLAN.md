---
phase: "08.2-audio-system-refactor"
plan: "02"
type: "execute"
wave: 2
depends_on: ["08.2-01"]
files_modified: ["src/components/properties/AudioTab.tsx", "src/components/editor/Timeline.tsx", "src/components/editor/AudioLibrary.tsx"]
autonomous: true
requirements: ["M3-ED-01", "M3-ED-02"]
must_haves:
  truths:
    - "UI labels say 'Audio' instead of 'BGM' in properties and timeline"
    - "Import Audio button opens Tauri file dialog and successfully imports a file"
    - "Timeline shows 'AUDIO' track label"
  artifacts:
    - path: "src/components/editor/Timeline.tsx"
      provides: "Updated track labels"
    - path: "src/components/editor/AudioLibrary.tsx"
      provides: "Import local audio button"
---

<objective>
Update the UI to reflect the new 'Audio' naming and implement a more accessible 'Import Audio' functionality similar to CapCut.

Purpose: Professional UI and easy local asset import.
Output: Refreshed UI with new labels and working import buttons.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/properties/AudioTab.tsx
@src/components/editor/Timeline.tsx
@src/components/editor/AudioLibrary.tsx
</context>

<tasks>

<task type="auto">
  <name>Rename UI Labels in AudioTab</name>
  <files>src/components/properties/AudioTab.tsx</files>
  <action>
    Rename "Background Music" to "Audio" or "Music".
    Update MusicNotes icon to SpeakerHigh if it feels more general.
    Ensure the file selection button label is "Import Audio" or "Select File".
    Update all references to renamed store fields (audio_enabled, etc.).
  </action>
  <verify>
    UI displays "Audio" instead of "Background Music".
  </verify>
  <done>AudioTab labels updated.</done>
</task>

<task type="auto">
  <name>Rename Timeline Track and Update Logic</name>
  <files>src/components/editor/Timeline.tsx</files>
  <action>
    Change the track label from "BGM" to "AUDIO".
    Update all logic references from bgm_* to audio_*.
    Ensure selection ID 'bgm-main' is also updated to 'audio-main' if applicable (and check where else this ID is used).
  </action>
  <verify>
    Timeline shows "AUDIO" and track functionality (drag, trim, delete) still works.
  </verify>
  <done>Timeline track renamed and logic updated.</done>
</task>

<task type="auto">
  <name>Implement Import Audio in AudioLibrary</name>
  <files>src/components/editor/AudioLibrary.tsx</files>
  <action>
    Add a prominent "Import Audio" button at the top of the Audio Library list.
    Use Tauri's `open` dialog API (from `@tauri-apps/plugin-dialog`) to pick audio files.
    When a file is selected, set it as the active audio track in the store.
  </action>
  <verify>
    Clicking "Import Audio" opens a dialog and updates the timeline.
  </verify>
  <done>Import Audio button added to Library.</done>
</task>

</tasks>

<success_criteria>
- No "BGM" text remains in the primary UI (replaced by "Audio").
- "Import Audio" button is functional in both Audio tab and Audio Library.
- Multi-track timeline still functions correctly for video, audio, and subtitles.
</success_criteria>

<output>
After completion, create .planning/phases/08.2-audio-system-refactor/08.2-02-SUMMARY.md
</output>
