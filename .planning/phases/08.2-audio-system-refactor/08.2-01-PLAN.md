---
phase: "08.2-audio-system-refactor"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["src/stores/useProjectStore.ts", "src/hooks/usePipeline.ts", "python-sidecar/app/pipeline_runner.py", "python-sidecar/app/engines/output.py", "python-sidecar/app/routes/pipeline.py"]
autonomous: true
requirements: ["M3-ED-01"]
must_haves:
  truths:
    - "Project store uses audio_* instead of bgm_* for all music/audio fields"
    - "Sidecar PipelineConfig and FFmpegOutputBuilder accept audio_* parameters"
    - "Video rendering still works with the new field names"
  artifacts:
    - path: "src/stores/useProjectStore.ts"
      provides: "Renamed audio state fields"
    - path: "python-sidecar/app/pipeline_runner.py"
      provides: "Renamed audio config fields"
  key_links:
    - from: "src/App.tsx"
      to: "renderVideo"
      via: "ProjectConfig object"
---

<objective>
Refactor the internal naming from 'BGM' to 'Audio' across the frontend store and backend sidecar to prepare for a more general audio system.

Purpose: Foundation for multi-audio support and professional naming.
Output: Unified naming convention in code and working rendering pipeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/stores/useProjectStore.ts
@python-sidecar/app/pipeline_runner.py
@python-sidecar/app/engines/output.py
</context>

<tasks>

<task type="auto">
  <name>Refactor useProjectStore.ts</name>
  <files>src/stores/useProjectStore.ts</files>
  <action>
    Rename all occurrences of 'bgm' to 'audio' in field names and logic:
    - bgm_enabled -> audio_enabled
    - bgm_file -> audio_file
    - bgm_volume -> audio_volume
    - bgm_clip_start -> audio_clip_start
    - bgm_clip_duration -> audio_clip_duration
    - bgm_timeline_start -> audio_timeline_start
    Update DEFAULT_CONFIG and state transition logic (splitLeft, splitRight).
    Ensure persistence key 'knreup-project-config' remains same but handle potential migration if needed (or just let users re-select music for this one-time change).
  </action>
  <verify>
    npm run build (to check for type errors)
  </verify>
  <done>Store fields renamed and logic updated.</done>
</task>

<task type="auto">
  <name>Refactor Sidecar Backend</name>
  <files>python-sidecar/app/pipeline_runner.py, python-sidecar/app/engines/output.py, python-sidecar/app/routes/pipeline.py</files>
  <action>
    Rename 'bgm' to 'audio' in:
    1. PipelineConfig class in pipeline_runner.py.
    2. FFmpegOutputBuilder build() method in output.py (where it reads from config).
    3. pipeline.py route handler where it parses config_json.
    Keep backward compatibility in kwargs if necessary, but preferred to do a clean rename since this is an internal API.
  </action>
  <verify>
    Run pytest on sidecar or manual test of render.
  </verify>
  <done>Sidecar fields renamed and rendering logic updated.</done>
</task>

<task type="auto">
  <name>Update usePipeline.ts types</name>
  <files>src/hooks/usePipeline.ts</files>
  <action>
    Update PipelineConfig interface to use audio_* naming instead of bgm_*.
  </action>
  <verify>
    Check for TS errors in App.tsx.
  </verify>
  <done>Pipeline hook types updated.</done>
</task>

</tasks>

<success_criteria>
- Project compiles without TS errors regarding renamed fields.
- Backend sidecar starts without errors.
- Rendering a video with background music still works correctly.
</success_criteria>

<output>
After completion, create .planning/phases/08.2-audio-system-refactor/08.2-01-SUMMARY.md
</output>
