---
wave: 3
depends_on: ["25-02-PLAN.md"]
files_modified: ["python-sidecar/app/engines/downloader/fb_engine.py", "python-sidecar/app/engines/downloader/manager.py"]
autonomous: true
---

# 25-03: Facebook & Hybrid Fallback Crawlers

## Objective
Implement Facebook extraction through direct video parsing, reducing reliance on yt-dlp which could fail on private/group videos if authenticated differently, and implement a hybrid fallback.

## Tasks

<task>
  <description>Create Facebook Engine Class</description>
  <read_first>["python-sidecar/app/engines/downloader/base.py"]</read_first>
  <action>
    Create `python-sidecar/app/engines/downloader/fb_engine.py`.
    Implement `FacebookEngine(BaseEngine)` which scrapes FB video headers for `hd_src_no_ratelimit` or GraphQL variables indicating raw mp4 streams.
    Implement fetching logic to determine the highest resolution stream available.
  </action>
  <acceptance_criteria>
    `fb_engine.py` exists with a working `FacebookEngine`.
  </acceptance_criteria>
</task>

<task>
  <description>Route Manager and Refine Hybrid Workflow</description>
  <read_first>["python-sidecar/app/engines/downloader/manager.py"]</read_first>
  <action>
    In `manager.py`, add `FacebookEngine` for `facebook.com` and `fb.watch` domains.
    Implement a strategy where if a native engine (`tiktok`, `bilibili`, `fb`) fails, it falls back to `ytdlp_engine.py` automatically before throwing an error.
  </action>
  <acceptance_criteria>
    Facebook links correctly route to `FacebookEngine`.
    Failing native engine queries fallback to `YTdlpEngine`.
  </acceptance_criteria>
</task>
