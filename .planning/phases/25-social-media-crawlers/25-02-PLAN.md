---
wave: 2
depends_on: ["25-01-PLAN.md"]
files_modified: ["python-sidecar/app/engines/downloader/bilibili_engine.py", "python-sidecar/app/engines/downloader/manager.py"]
autonomous: true
---

# 25-02: Bilibili & Facebook Crawlers

## Objective
Add specific extractors for Bilibili and Facebook to provide fast API-level metadata fetching and high-quality download capabilities.

## Tasks

<task>
  <description>Implement Bilibili Engine</description>
  <read_first>["python-sidecar/app/engines/downloader/base.py"]</read_first>
  <action>
    Create `bilibili_engine.py` containing `BilibiliEngine(BaseEngine)`.
    Implement extraction of the Bvid. Fetch the meta-data using Bilibili's public API (e.g. `api.bilibili.com/x/web-interface/view?bvid={bvid}`).
    Retrieve video/audio play URLs using `api.bilibili.com/x/player/playurl`.
    Extract the DASH audio and video streams (since Bilibili separates video and audio at higher resolutions).
  </action>
  <acceptance_criteria>
    `bilibili_engine.py` is fully implemented and can resolve Bilibili URLs.
  </acceptance_criteria>
</task>

<task>
  <description>Update Routing for Bilibili</description>
  <read_first>["python-sidecar/app/engines/downloader/manager.py"]</read_first>
  <action>
    In `manager.py`, add the `BilibiliEngine` to the routing logic for `bilibili.com` and `b23.tv`.
  </action>
  <acceptance_criteria>
    `manager.py` routes `bilibili.com` domains to `BilibiliEngine`.
  </acceptance_criteria>
</task>
