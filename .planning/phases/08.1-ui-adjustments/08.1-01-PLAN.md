---
phase: 08.1-ui-adjustments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/editor/VideoControls.tsx, src/components/editor/Timeline.tsx, src/utils/time.ts]
autonomous: true
requirements: [UI-FIX-01, UI-FIX-02, TIME-FIX-01]

must_haves:
  truths:
    - "VideoControls time display has reduced width and no extra right margin"
    - "Timeline header no longer shows time display in the center"
    - "Time format is mm:ss:ff (minutes:seconds:frames)"
  artifacts:
    - path: "src/components/editor/VideoControls.tsx"
      provides: "Adjusted minWidth for time display"
    - path: "src/components/editor/Timeline.tsx"
      provides: "Cleaned up header"
    - path: "src/utils/time.ts"
      provides: "New formatTime implementation"
---

<objective>
Fix UI issues in VideoControls and Timeline, and update the time formatting to support frames (mm:ss:ff) as requested by the user.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/editor/VideoControls.tsx
@src/components/editor/Timeline.tsx
@src/utils/time.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjust VideoControls and Timeline UI</name>
  <files>src/components/editor/VideoControls.tsx, src/components/editor/Timeline.tsx</files>
  <action>
    1. In `src/components/editor/VideoControls.tsx`:
       - Change `minWidth: 160` to `minWidth: 100` in the `.tcd` div style to remove extra space.
    2. In `src/components/editor/Timeline.tsx`:
       - Remove the `div` containing the center time display (currentTime / videoDuration) from the `tlhd` section.
  </action>
  <verify>
    - Check `src/components/editor/VideoControls.tsx` for `minWidth: 100`.
    - Check `src/components/editor/Timeline.tsx` and ensure the center time display block is gone.
  </verify>
  <done>UI elements adjusted according to user feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Update time formatting to mm:ss:ff</name>
  <files>src/utils/time.ts</files>
  <action>
    Update `formatTime` in `src/utils/time.ts` to return the format `mm:ss:ff`.
    - `mm`: total minutes (including hours if any, e.g., 75 minutes for 1h15m).
    - `ss`: seconds.
    - `ff`: frames (based on fps, default 30).
    - Use `Math.floor(secs / 60)` for minutes.
    - Use `Math.floor(secs % 60)` for seconds.
    - Use `Math.floor((secs % 1) * fps)` for frames.
  </action>
  <verify>
    - Test `formatTime(214.76, 30)` -> should be "03:34:22" (approx, 0.76 * 30 = 22.8).
    - Test `formatTime(0)` -> "00:00:00".
  </verify>
  <done>Time formatting updated to support frames in mm:ss:ff format.</done>
</task>

</tasks>

<success_criteria>
- VideoControls time display is more compact.
- Timeline header center is empty.
- formatTime returns mm:ss:ff.
</success_criteria>

<output>
After completion, create .planning/phases/08.1-ui-adjustments/08.1-01-SUMMARY.md
</output>
