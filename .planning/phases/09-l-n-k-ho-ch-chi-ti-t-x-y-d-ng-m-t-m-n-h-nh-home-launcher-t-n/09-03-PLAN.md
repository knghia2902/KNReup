---
phase: "09"
plan: "03"
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified: [
  "src/components/downloader/DownloaderWindow.tsx",
  "src/components/tools/VoiceCloneWindow.tsx",
  "src/components/layout/Titlebar.tsx",
  "src/components/editor/PropertiesPanel.tsx",
  "src/utils/windowManager.ts"
]
autonomous: true
requirements: [M3-HOME-04, M3-HOME-03]
must_haves:
  truths:
    - "Công cụ Downloader mở trong cửa sổ popup riêng thay vì tab Editor"
    - "Tính năng Voice Clone hiển thị UI placeholder trong cửa sổ mới"
    - "Người dùng Guest không thể nhấn các nút premium mà không có thông báo auth"
  artifacts:
    - path: "src/components/downloader/DownloaderWindow.tsx"
      provides: "Entry point cho cửa sổ downloader"
    - path: "src/components/tools/VoiceCloneWindow.tsx"
      provides: "Entry point cho cửa sổ voice clone"
---

<objective>
Tách các công cụ thành cửa sổ độc lập và áp dụng Auth Guard cho các tính năng premium.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/09-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Tách Downloader thành Popup Window</name>
  <files>src/components/downloader/DownloaderWindow.tsx, src/utils/windowManager.ts, src/components/layout/Titlebar.tsx</files>
  <action>
    - Tạo `DownloaderWindow.tsx` làm entry point chính cho module Downloader.
    - Cập nhật `windowManager.ts` thêm `openDownloader()` với kích thước mặc định (ví dụ 1000x700).
    - Trong `Titlebar.tsx`, đổi tab "Downloader" thành một nút bấm gọi `windowManager.openDownloader()`.
    - Ẩn tab Downloader trong `NLELayout` nếu đang ở chế độ Editor.
  </action>
  <verify>
    <automated>Verify that clicking Downloader opens a new window and content is visible</automated>
  </verify>
  <done>Module Downloader hoạt động độc lập.</done>
</task>

<task type="auto">
  <name>Tạo UI Placeholder cho Voice Clone</name>
  <files>src/components/tools/VoiceCloneWindow.tsx, src/App.tsx</files>
  <action>
    - Xây dựng UI cơ bản cho Voice Clone (Tiêu đề, hướng dẫn upload giọng mẫu).
    - Đăng ký routing trong `App.tsx` cho label `tool-voice-clone`.
    - Kết nối nút Voice Clone trên Launcher với `windowManager.openTool('voice-clone')`.
  </action>
  <verify>
    <automated>Verify Voice Clone window opens from Launcher</automated>
  </verify>
  <done>Công cụ Voice Clone sẵn sàng phát triển logic.</done>
</task>

<task type="auto">
  <name>Áp dụng RequireAuth cho Feature Premium</name>
  <files>src/components/editor/PropertiesPanel.tsx, src/components/setup/HomeLauncher.tsx</files>
  <action>
    - Sử dụng component `RequireAuth` (đã tạo ở Plan 01) bọc các nút:
      - "AI Refine" (Subtitle).
      - "Voice Clone" (Launcher/Editor).
      - "Translate with Gemini/OpenAI".
    - Khi click vào mà chưa login, hiện Toast hoặc Modal yêu cầu đăng nhập.
  </action>
  <verify>
    <automated>Verify that premium buttons show auth requirement UI for guest users</automated>
  </verify>
  <done>Chế độ Guest Mode được thực thi đúng theo D-02.</done>
</task>
</tasks>

<success_criteria>
- Launcher trở thành trung tâm điều phối (Hub) cho các công cụ.
- Editor tập trung vào chỉnh sửa video.
- Hệ thống Auth Guard bảo vệ các tài nguyên premium.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/09-03-SUMMARY.md`
</output>
