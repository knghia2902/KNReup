---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [
  "src/components/setup/HomeLauncher.tsx",
  "src/stores/useLauncherStore.ts",
  "src/components/setup/RecentProjects.tsx",
  "src/stores/useProjectStore.ts",
  "src/styles/launcher.css"
]
autonomous: true
requirements: [M3-HOME-02]
must_haves:
  truths:
    - "Giao diện Launcher hiển thị các nút chức năng chính (Start, Tools)"
    - "Danh sách dự án gần đây hiển thị theo dạng lưới với Thumbnail"
    - "Dữ liệu dự án gần đây được lưu trữ và khôi phục khi khởi động app"
  artifacts:
    - path: "src/components/setup/HomeLauncher.tsx"
      provides: "Giao diện chính của Launcher"
    - path: "src/stores/useLauncherStore.ts"
      provides: "Quản lý danh sách dự án"
  key_links:
    - from: "src/components/setup/HomeLauncher.tsx"
      to: "src/utils/windowManager.ts"
      via: "Nút Start Creating mở Editor mới"
---

<objective>
Xây dựng giao diện Home Launcher và logic quản lý danh sách dự án gần đây.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/09-CONTEXT.md
@.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Tạo useLauncherStore & UI RecentProjects</name>
  <files>src/stores/useLauncherStore.ts, src/components/setup/RecentProjects.tsx</files>
  <action>
    - Tạo `useLauncherStore` với mảng `recentProjects: ProjectMetadata[]`.
    - Dùng Zustand `persist` để lưu danh sách này.
    - Xây dựng component `RecentProjects` hiển thị Grid (3-4 cột) các project card (Thumbnail, Name, Path, Last Modified).
    - **Wiring:** Khi người dùng click vào một project card, gọi hàm `windowManager.openEditor(project.id)` để mở dự án đó trong cửa sổ mới.
  </action>
  <verify>
    <automated>Verify useLauncherStore can add and retrieve projects, and card clicks trigger windowManager</automated>
  </verify>
  <done>Store và UI dự án gần đây hoàn thiện với đầy đủ tương tác.</done>
</task>

<task type="auto">
  <name>Hoàn thiện HomeLauncher Dashboard</name>
  <files>src/components/setup/HomeLauncher.tsx, src/styles/launcher.css</files>
  <action>
    - Thiết kế Layout theo CapCut:
      - Sidebar hẹp (optional) hoặc Top-bar.
      - Center: Nút lớn "Bắt đầu tạo video".
      - Bottom: Grid `RecentProjects`.
      - Right/Left: List các "Tools" (Downloader, v.v.).
    - Style dùng Glassmorphism và màu sắc hiện đại (`design-system.css`).
  </action>
  <verify>
    <automated>Check HomeLauncher.tsx renders all major sections</automated>
  </verify>
  <done>Giao diện Launcher sẵn sàng sử dụng.</done>
</task>

<task type="auto">
  <name>Tự động cập nhật Thumbnail dự án</name>
  <files>src/stores/useProjectStore.ts, src/App.tsx</files>
  <action>
    - Thêm logic vào `useProjectStore` hoặc `App.tsx`: Khi save dự án, nếu có video đang mở, gọi API sidecar `/api/pipeline/thumbnail` để chụp ảnh frame tại playhead.
    - Lưu ảnh vào thư mục cache của App và cập nhật đường dẫn vào `useLauncherStore`.
  </action>
  <verify>
    <automated>Verify that saving a project adds/updates an entry in useLauncherStore with a thumbnail path</automated>
  </verify>
  <done>Hệ thống tự động hóa metadata dự án hoạt động.</done>
</task>
</tasks>

<success_criteria>
- Launcher hiển thị trực quan và chuyên nghiệp.
- Danh sách dự án gần đây cập nhật thời gian thực.
- Ảnh Thumbnail giúp người dùng nhận diện dự án nhanh chóng.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/09-02-SUMMARY.md`
</output>
