---
phase: "09"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/stores/useAuthStore.ts",
  "src/components/auth/RequireAuth.tsx",
  "src/utils/windowManager.ts",
  "src-tauri/tauri.conf.json",
  "src/App.tsx",
  "src/main.tsx"
]
autonomous: true
requirements: [M3-HOME-01, M3-HOME-03]
must_haves:
  truths:
    - "Ứng dụng khởi động vào giao diện Launcher thay vì Editor"
    - "Có thể mở một cửa sổ Editor mới từ Launcher"
    - "Hệ thống nhận diện được trạng thái Guest/User"
  artifacts:
    - path: "src/stores/useAuthStore.ts"
      provides: "Quản lý trạng thái đăng nhập"
    - path: "src/utils/windowManager.ts"
      provides: "Tiện ích mở cửa sổ Tauri 2.0"
  key_links:
    - from: "src/main.tsx"
      to: "src/utils/windowManager.ts"
      via: "Kiểm tra window label để điều hướng"
---

<objective>
Thiết lập kiến trúc đa cửa sổ (Multi-window) và hệ thống phân quyền (Auth Store) làm nền tảng cho Home Launcher.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/09-CONTEXT.md
@.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Khởi tạo useAuthStore & RequireAuth</name>
  <files>src/stores/useAuthStore.ts, src/components/auth/RequireAuth.tsx</files>
  <action>
    - Tạo `useAuthStore` dùng Zustand `persist` để quản lý: `user` (null), `isLoggedIn` (boolean).
    - Mặc định khởi tạo là Guest Mode (`isLoggedIn: false`).
    - Tạo component `RequireAuth` nhận `children` và `fallback?`. Nếu không logged in, hiển thị UI thông báo yêu cầu đăng nhập thay vì children.
  </action>
  <verify>
    <automated>Check if files exist and export useAuthStore and RequireAuth</automated>
  </verify>
  <done>Hệ thống Auth cơ bản sẵn sàng.</done>
</task>

<task type="auto">
  <name>Cấu hình Multi-window & Window Manager</name>
  <files>src-tauri/tauri.conf.json, src/utils/windowManager.ts</files>
  <action>
    - Cập nhật `src-tauri/tauri.conf.json`: Đặt `label: "launcher"` cho cửa sổ mặc định.
    - Tạo `src/utils/windowManager.ts` sử dụng `@tauri-apps/api/webviewWindow`.
    - Triển khai hàm `openEditor(projectId?: string)` tạo cửa sổ mới với label `editor-${id}` và URL `index.html?type=editor&id=${id}`.
    - Triển khai hàm `openTool(toolId: string)` tạo cửa sổ popup.
  </action>
  <verify>
    <automated>Verify tauri.conf.json has launcher label and windowManager.ts exports openEditor</automated>
  </verify>
  <done>Window Manager có thể khởi tạo các cửa sổ độc lập.</done>
</task>

<task type="auto">
  <name>Cập nhật Entry Point & Routing</name>
  <files>src/main.tsx, src/App.tsx</files>
  <action>
    - Trong `main.tsx` hoặc `App.tsx`, sử dụng `getCurrentWindow().label` để xác định view.
    - Nếu label là `launcher` -> Render `HomeLauncher`.
    - Nếu label bắt đầu bằng `editor` -> Render layout Editor.
    - **Quan trọng:** Trong view Editor, thực hiện parse `id` từ `window.location.search` (ví dụ: `new URLSearchParams(window.location.search).get('id')`) và gọi `loadProject(id)` từ `useProjectStore` để khôi phục dữ liệu dự án.
    - Đảm bảo các provider (Zustand, QueryClient) bao bọc cả hai trường hợp.
  </action>
  <verify>
    <automated>Run app and verify it shows Launcher placeholder (if label is launcher) and Editor parses ID if label is editor</automated>
  </verify>
  <done>App phân tách được view dựa trên window context và khôi phục dữ liệu dự án thành công.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Window -> OS | Window creation requests cross to Tauri core |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | WindowManager | mitigate | validate window labels and URLs before creation |
</threat_model>

<success_criteria>
- Ứng dụng khởi động vào "Launcher".
- Có thể dùng console hoặc nút tạm để mở cửa sổ "Editor" mới.
- Trạng thái đăng nhập được lưu trữ bền vững.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-l-n-k-ho-ch-chi-ti-t-x-y-d-ng-m-t-m-n-h-nh-home-launcher-t-n/09-01-SUMMARY.md`
</output>
