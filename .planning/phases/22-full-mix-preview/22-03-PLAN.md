---
phase: 22-full-mix-preview
gap_closure: true
status: planned
---

# 22-03-PLAN: Refactoring Audio Volume Controls (UI Adjustments)

### Issues Addressed (from 22-UAT.md)
1. **Test 3:** BGM Volume slider should be moved to the Audio Tab (properties panel) so all volume controls are centralized.
2. **Test 4:** Timeline Mini Volume Indicators on track headers (Timeline.tsx) should be removed to match CapCut's design, keeping only the mute/show toggles.

### Proposed Changes

#### 1. Modify src/components/properties/AudioTab.tsx
- Add an import for \MusicNotes\ from \@phosphor-icons/react\.
- Inject a new section at the bottom for **Background Music**.
- Connect the **Background Music** section to \config.audio_volume\ and \config.ducking_strength\.

#### 2. Modify src/components/editor/AudioLibrary.tsx
- Remove the \SliderControl\ usages for \Volume\ and \Ducking\.
- Keep the \<audio>\ element for previewing the track.

#### 3. Modify src/components/editor/Timeline.tsx
- In the track headers mapping loop (lines 501-551), remove the draggable progress bar \<div onPointerDown={...}>\ logic used for tracking volume.
- Retain the mute icon functionality (the \config[tk.volumeKey]\ bars disappear, just keep the speaker/muted icon that users can optionally toggle, though right now it just displays state).
