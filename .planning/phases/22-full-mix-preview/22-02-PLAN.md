---
wave: 1
gap_closure: true
autonomous: false
files_modified:
  - src/lib/audioMixer.ts
  - src/components/editor/VideoPreview.tsx
  - src/components/editor/Timeline.tsx
  - src/components/editor/AudioLibrary.tsx
---

# Phase 22: Gap Closure Plan

<objective>
Fix the 4 UAT issues identified during the Full Mix Preview testing: sync BGM with VideoPreview timeline play, add mouse interaction to Timeline mini volume indicators, add temporary complete mute during timeline scrubbing, and fix audio leak across tab changes.
</objective>

<tasks>

<task>
<description>Implement muteAll toggle in AudioMixer for scrubbing</description>
<read_first>
- src/lib/audioMixer.ts
</read_first>
<action>
In `src/lib/audioMixer.ts`:
1. Add a `muteAll(shouldMute: boolean)` method to the `AudioMixer` class.
2. When `shouldMute` is true, temporarily set the target gain of `bgmGainNode`, `originalAudioGain` (or context volume) to 0. Store the previous volume states.
3. When `shouldMute` is false, restore the previous targets (e.g. `setBgmVolume(previousBgmVolume)`).
</action>
<acceptance_criteria>
`src/lib/audioMixer.ts` contains `muteAll(shouldMute: boolean)` method which modifies gain nodes to 0 and restores them.
</acceptance_criteria>
</task>

<task>
<description>Mute audio during timeline scrubbing</description>
<read_first>
- src/components/editor/Timeline.tsx
- src/lib/audioMixer.ts
</read_first>
<action>
In `src/components/editor/Timeline.tsx`:
1. Inside the `PointerDown` handler that initiates scrubbing, call `AudioMixer.muteAll(true)` along with `cancelTTS()`.
2. Inside the pointer up/release handler that finishes scrubbing, call `AudioMixer.muteAll(false)` and then schedule audio/resume play.
</action>
<acceptance_criteria>
`src/components/editor/Timeline.tsx` contains a call to `muteAll(true)` on pointer down and `muteAll(false)` on pointer up.
</acceptance_criteria>
</task>

<task>
<description>Synchronize BGM playback with VideoPreview</description>
<read_first>
- src/components/editor/VideoPreview.tsx
- src/components/editor/AudioLibrary.tsx
- src/lib/audioMixer.ts
</read_first>
<action>
In `src/components/editor/VideoPreview.tsx`:
1. Observe the `projectConfig.audio_path` and `isPlaying` state.
2. When the video plays (`videoRef.current.play()`), if `audio_path` is present, it should be scheduled/played via `AudioMixer` using the current video timestamp, or using an invisible `<audio>` element that is `.play()`-ed and `.pause()`-ed in perfect sync with the video element.
3. If using an invisible audio element, hook it up to the `AudioMixer` BGM gain node so its volume is controlled.

In `src/components/editor/AudioLibrary.tsx`:
1. Temporarily pause AudioLibrary previews if `isPlaying` from `useProjectStore` is true. Ensure the Library is strictly for previewing Library items, not for running project BGM playback on the timeline.
</action>
<acceptance_criteria>
`src/components/editor/VideoPreview.tsx` handles `audio_path` playback synchronized with video play/pause events.
</acceptance_criteria>
</task>

<task>
<description>Make Timeline Mini Volume Indicators interactive</description>
<read_first>
- src/components/editor/Timeline.tsx
</read_first>
<action>
In `src/components/editor/Timeline.tsx`:
1. Attach `onPointerDown`, `onPointerMove`, and `onPointerUp` events to the visual volume bars (Original Video, TTS, BGM tracks) in the track headers.
2. Calculate the volume percentage based on the pointer's X coordinate relative to the bar's bounding client rect.
3. Dispatch `updateProjectConfig` with the new volume levels (`original_volume`, `volume`, `audio_volume`) matching the calculated values (0-1).
</action>
<acceptance_criteria>
`src/components/editor/Timeline.tsx` volume bars possess pointer event handlers that call `updateProjectConfig`.
</acceptance_criteria>
</task>

<task>
<description>Stop audio on tab unmount/visibility change</description>
<read_first>
- src/components/editor/VideoPreview.tsx
</read_first>
<action>
In `src/components/editor/VideoPreview.tsx`:
1. Use `useEffect` hook to pause video (`videoRef.current.pause()`) and stop `AudioMixer` BGM on component unmount to ensure audio doesn't leak when navigating to the Downloader or Settings tab via the Home Launcher.
</action>
<acceptance_criteria>
`src/components/editor/VideoPreview.tsx` calls `.pause()` on video and BGM in a `useEffect` cleanup function.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Click play: BGM and Video play simultaneously.
2. Scrub playhead: No glitched audio sounds while dragging; audio resumes when finger lifted.
3. Click and drag the volume progress bars in the Timeline headers; the bars move and the config is updated.
4. Go to Downloader while Editor is playing; audio stops.
</verification>
