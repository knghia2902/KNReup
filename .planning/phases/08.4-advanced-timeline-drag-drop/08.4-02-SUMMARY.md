---
phase: "08.4"
plan: 2
title: "Insert Between Clips & Ghost Preview"
status: complete
started: "2026-04-27T15:35:00+07:00"
completed: "2026-04-27T15:50:00+07:00"
---

# Summary: Insert Between Clips & Ghost Preview

## What was built

Logic insert between clips, ghost preview, và insert indicator đã được integrated trực tiếp vào Plan 01 implementation (cùng commit `bb48407`) vì tất cả share chung `Timeline.tsx` và `useTimelineStore.ts`.

### Implemented in Plan 01:
1. **Ghost preview** — hình chữ nhật mờ (opacity 0.45, border dashed) theo chuột khi kéo clip
2. **Insert indicator** — vạch xanh (#22c55e) pulsing tại vị trí gap giữa clips
3. **`detectInsertPosition()`** — tìm gap gần nhất giữa clips với threshold 0.5s
4. **`rippleInsert()`** — chèn clip vào vị trí, dồn clips phía sau ra
5. **Track highlight** — background `rgba(99,102,241,0.1)` với transition 0.15s
6. **pulse-insert CSS** — @keyframes animation cho insert indicator

### Deviation from plan:
- Không tạo separate useTimelineDrag hook state — ghost/insert state được quản lý trực tiếp trong Timeline.tsx component để tránh complexity không cần thiết
- Hợp nhất vào Plan 01 vì share 100% files_modified

## Self-Check: PASSED
