---
wave: 1
depends_on: []
files_modified:
  - "src/components/tools/VoiceStudioWindow.tsx"
  - "src/components/tools/voicestudio/CloneTab.tsx"
  - "src/components/tools/voicestudio/DesignTab.tsx"
  - "src/stores/useVoiceStudioStore.ts"
  - "python-sidecar/app/routes/voice_studio.py"
  - "python-sidecar/app/engines/tts/omnivoice_engine.py"
autonomous: true
---

# Phase 12: Remove OmniVoice Clone & Design Features

<goal>
Gỡ bỏ hoàn toàn tính năng Voice Clone và Voice Design từ UI, Backend Config và dọn dẹp mã nguồn. Giữ lại mô hình `splendor1811/omnivoice-vietnamese` dùng làm Text-to-Speech Engine.
</goal>

## Task 1: Dọn dẹp Backend (Python Sidecar)
<task>
<read_first>
- python-sidecar/app/routes/voice_studio.py
- python-sidecar/app/engines/tts/omnivoice_engine.py
</read_first>

<action>
1. Trong `omnivoice_engine.py`: Xóa hàm `generate_from_audio()` và `generate_from_description()` (Voice Clone và Voice Design) hoàn toàn. Chỉ giữ lại `generate_audio()` (TTS mặc định).
2. Trong `voice_studio.py`: Nếu có các endpoint cho Clone và Design thì xóa bỏ.
</action>

<acceptance_criteria>
- Hàm `generate_from_audio` không còn tồn tại trong `omnivoice_engine.py`.
- Hàm `generate_from_description` không còn tồn tại trong `omnivoice_engine.py`.
</acceptance_criteria>
</task>

## Task 2: Dọn dẹp Frontend Store
<task>
<read_first>
- src/stores/useVoiceStudioStore.ts
</read_first>

<action>
1. Xóa bỏ mọi references, state, và API functions liên quan tới Voice Clone (ví dụ `cloneVoice` API call) và Voice Design (ví dụ `designVoice` API call) trong Zustand store.
</action>

<acceptance_criteria>
- File `useVoiceStudioStore.ts` không chứa từ khóa `cloneVoice` hoặc chức năng gọi API upload clone.
</acceptance_criteria>
</task>

## Task 3: Dọn dẹp Component & UI
<task>
<read_first>
- src/components/tools/VoiceStudioWindow.tsx
- src/components/tools/voicestudio/CloneTab.tsx
- src/components/tools/voicestudio/DesignTab.tsx
</read_first>

<action>
1. Xóa các file `CloneTab.tsx` và `DesignTab.tsx` thông qua lệnh shell hoặc git rm.
2. Trong `VoiceStudioWindow.tsx`:
   - Xóa các Import cho `CloneTab` và `DesignTab`.
   - Chỉ giữ lại giao diện TTS và HistoryPanel.
   - Ẩn phần chuyển đổi "Tab" giữa TTS/Clone/Design nếu nó đang chiếm diện tích. Setup default `activeTab` sang `tts` và loại bỏ khả năng Set tab.
</action>

<acceptance_criteria>
- Lệnh `ls src/components/tools/voicestudio/CloneTab.tsx` trả về lỗi không tìm thấy file.
- `VoiceStudioWindow.tsx` được render thành 1 layout bao gồm 1 Form TTS bên trái và History Panel bên phải, không chứa Header Toggle.
</acceptance_criteria>
</task>

<verification>
1. Chạy `npm run tauri dev` cùng với Python Sidecar (`py run_dev.py`).
2. Mở ứng dụng, chọn công cụ Voice Studio từ Home Launcher.
3. Cửa sổ Voice Studio chỉ hiển thị phần Text-to-Speech và danh sách Lịch Sử. Không còn các tuỳ chọn Voice Clone hay Design.
4. Thử Generate 1 đoạn TTS sử dụng engine `OmniVoice` -> Thành công.
</verification>
<must_haves>
- Xóa UI Clone / Design.
- Xóa cấu hình / Logic rác trên backend của k2-fsa/Omnivoice.
- Text-to-Speech (splendor1811) hoạt động tốt.
</must_haves>
